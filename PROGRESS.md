# Bimeup — Progress Tracker

## Current Stage: RP — Render Polish (non-RT beauty pass)
## Current Task: RP.10 Small-kernel bloom

## Completed Tasks
<!-- Mark tasks as they are done: - [x] 1.1 Description -->

## Stage 1 — Project Bootstrap & Build System
- [x] 1.1 Create root CMakeLists.txt with C++20, module structure, option flags
- [x] 1.2 Add git submodules: googletest, spdlog, glm
- [x] 1.3 Create tools/ module with Log wrapper around spdlog
- [x] 1.4 Create tools/Config — key-value config loader
- [x] 1.5 Create app/main.cpp — initializes logging, prints version
- [x] 1.6 Create build scripts (debug/release for Linux/Windows)
- [x] 1.7 Set up GitHub Actions CI
- [x] 1.8 Enable clang-tidy and sanitizers

## Stage 2 — Platform Layer & Window
- [x] 2.1 Add GLFW submodule, create platform/Window class
- [x] 2.2 Create platform/Input — keyboard + mouse abstraction
- [x] 2.3 Create Vulkan instance + debug messenger
- [x] 2.4 Create Vulkan physical/logical device selection
- [x] 2.5 Create swapchain tied to GLFW surface
- [x] 2.6 Implement render loop: acquire → clear → present
- [x] 2.7 Implement frame timing and FPS tracking

## Stage 3 — Basic Rendering Pipeline
- [x] 3.1 Implement renderer/Buffer — vertex + index with VMA
- [x] 3.2 Implement renderer/Shader — load SPIR-V
- [x] 3.3 Implement renderer/Pipeline — graphics pipeline
- [x] 3.4 Implement renderer/DescriptorSet — UBO binding
- [x] 3.5 Implement renderer/Camera — perspective + orbit
- [x] 3.6 Write basic vertex + fragment shaders
- [x] 3.7 Render a colored cube with orbit camera
- [x] 3.8 Implement renderer/MeshBuffer
- [x] 3.9 Add render mode switching: shaded / wireframe

## Stage 4 — IFC Loading & Internal Scene
- [x] 4.1 Integrate IFC parsing library as submodule
- [x] 4.2 Create ifc/IfcModel
- [x] 4.3 Create ifc/IfcElement
- [x] 4.4 Create ifc/IfcGeometryExtractor
- [x] 4.5 Create ifc/IfcHierarchy
- [x] 4.6 Create scene/SceneNode and scene/Scene
- [x] 4.7 Create scene/SceneMesh
- [x] 4.8 Create scene/AABB
- [x] 4.9 Create scene/SceneBuilder
- [x] 4.10 Implement batching in SceneBuilder
- [x] 4.11 Upload scene meshes to renderer
- [x] 4.12 Implement per-element color from IFC

## Stage 5 — Core Application & Selection
- [x] 5.1 Create core/Application
- [x] 5.2 Create core/EventBus
- [x] 5.3 Define core events
- [x] 5.4 Implement CPU raycasting
- [x] 5.5 Mouse click → ray → selection pipeline
- [x] 5.6 Element highlighting in renderer
- [x] 5.7 Hover highlighting
- [x] 5.8 Multi-selection and clearing

## Stage 6 — ImGui Integration & Basic UI
- [x] 6.1 Integrate Dear ImGui with Vulkan + GLFW backend
- [x] 6.2 Create ui/UIManager
- [x] 6.3 Create ui/Panel interface
- [x] 6.4 Implement ui/HierarchyPanel
- [x] 6.5 Implement ui/PropertyPanel
- [x] 6.6 Implement ui/Toolbar
- [x] 6.7 Implement ui/ViewportOverlay
- [x] 6.8 Wire hierarchy → selection → property panel
- [x] 6.9 Implement UI theme
- [x] 6.10 Wire UI panels (UIManager, Toolbar, Hierarchy, Property, ViewportOverlay) into main.cpp
- [x] 6.11 Fix toolbar fit-to-view zoom + wire hover picking into main.cpp
- [x] 6.12 Enable fillModeNonSolid, fix surface/swapchain destroy order, per-image swapchain semaphores
- [x] 6.13 Replace demo cube with bundled sample.ifc as default startup scene

## Stage 7 — BIM Viewer Features
- [x] 7.0 Fix element selection (nodeId→expressId + batching-aware raycast)
- [x] 7.0a Fix projection double Y-flip (model was rendered upside down, picking desynced) + wire selection → visible vertex-color highlight via MeshBuffer::SetVertexColorOverride
- [x] 7.1 Distance measurement tool (scene::Measure + MeasureTool state machine)
- [x] 7.2 Point snapping (scene::Snap — vertex/edge/face)
- [x] 7.3 Clipping planes
  - [x] 7.3a Math — `renderer::ClipPlane` struct + `SignedDistance`/`ClassifyPoint` helpers + unit tests
  - [x] 7.3b `ClipPlaneManager` (up to 6 planes, add/remove/enable/update) + unit tests
  - [x] 7.3c UBO + shader integration (`ClipPlanesUbo` + `PackClipPlanes` + `basic.frag` discard + descriptor binding 3)
  - [x] 7.3d UI panel to edit planes interactively (`ui::ClipPlanesPanel` — add via ±X/Y/Z presets, enable/disable, edit normal+offset, remove; wired in `main.cpp`)
  - [x] 7.3e ImGuizmo integration — submodule, UIManager view/proj, clip plane Manipulate, ViewManipulate cube
    - [x] 7.3e.1 Add ImGuizmo submodule under `external/ImGuizmo`, compile into `imgui` target, smoke test links `ImGuizmo::IsUsing/IsOver`
    - [x] 7.3e.2 Thread view/proj matrices into `UIManager` (`SetCameraMatrices` / `GetViewMatrix` / `GetProjectionMatrix`), pushed each frame from `main.cpp`
    - [x] 7.3e.3 Translate/rotate gizmo for active clip plane (`renderer::PlaneToTransform` / `TransformToPlane` + `ImGuizmo::Manipulate` in `ClipPlanesPanel`)
    - [x] 7.3e.4 `ViewManipulate` cube wired to `Camera` orientation (`Camera::SetYawPitch` + `renderer::YawPitchFromForward`, called after `uiManager.BeginFrame()` in `main.cpp`)
- [x] 7.4 Plan view (`ui::PlanViewPanel` — Ground Floor / Roof switcher; creates horizontal clip plane at `elevation + cutAbove` and switches camera to top-down ortho framed to scene XZ; default sample swapped to `sample_house.ifc`)
  - [x] 7.4a Honor viewport aspect so ortho frame isn't stretched
  - [x] 7.4b Force yaw=0 on activate so plan isn't rotated; middle-mouse orbit auto-exits plan view
  - [x] 7.4c `Camera::Zoom` scales `m_orthoHeight` so scroll-zoom works in ortho (plan view + any future ortho mode)
  - [x] 7.4d Restore perspective on Deactivate; ImGuizmo ViewManipulate cube also auto-exits; ortho zoom rate doubled
  - [x] 7.4e `core::SelectionCleared` event — non-additive click on empty space clears selection + PropertyPanel (additive miss preserves)
- [x] 7.5 Section view (BRep approach — see `docs/section_cap_next_session.md`)
  - [x] 7.5a `ClipPlane` section fields (`sectionFill`, `fillColor`) + manager setters
  - [x] 7.5b `ClipPlanesPanel` "Section fill" checkbox + ColorEdit4 wired to setters
  - [x] 7.5c `scene::SliceTriangle` — plane/triangle intersection math (`scene/Slicing.{h,cpp}`; 8 unit tests covering all-front/back, coplanar, 2-1 splits, vertex-on-plane straddle/no-straddle, edge-on-plane)
  - [x] 7.5d `scene::SliceSceneMesh` — walk mesh triangles, transform, collect segments
  - [x] 7.5e `scene::StitchSegments` — endpoint hash-grid stitch into closed polygons (`scene/Slicing.{h,cpp}`; 7 unit tests covering empty input, square stitch, shuffled+flipped, two disjoint loops, open polyline drop, epsilon snap, cube slice → 8-vert closed loop)
  - [x] 7.5f `scene::TriangulatePolygon` — ear-clipping (project to dominant 2D plane, CCW-normalize, O(n²) ear-clip; `scene/Slicing.{h,cpp}`; 6 unit tests covering degenerate input, CCW/CW squares, concave L-shape, oblique plane, vertex-preservation)
  - [x] 7.5g `scene::SectionCapGeometry` — per-scene cap vertex buffer + dirty tracking (`BuildSectionCapVertices` walks planes × visible mesh-bearing nodes, slice → stitch → triangulate → tinted vertex list; class wraps `renderer::Buffer` with hash-based no-op `Rebuild` + `MarkDirty`; in scene/ namespace because scene → renderer dep direction forbids a Scene-aware class inside renderer/. 8 unit tests.)
  - [x] 7.5h `section_fill.{vert,frag}` shaders + `renderer::SectionFillPipeline` (flat-color pipeline: position+color vertex input, depth-test LEQUAL/write-off, CULL_NONE, no stencil, single camera UBO set; 4 Vulkan integration tests for shader-SPIR-V presence, construction, MSAA 4×, RAII)
  - [x] 7.5i Wire `SectionCapGeometry` + `SectionFillPipeline` into `main.cpp` (section-fill pipeline built next to shaded/wire, rebuilt on MSAA change; per-frame rebuild is hash-gated and skipped unless ≥1 plane has `enabled && sectionFill`; draws caps after scene, before UI, using main 4-binding descriptor set — shader reads only binding 0)
  - [x] 7.5k Per-element capping for batched IFC meshes (`BuildSectionCapVertices` now partitions batched-mesh triangles by `triangleOwners[t]`, slice→stitch→triangulate per owner, tint = `ownerVertexColor * plane.fillColor`). Open polylines fallback via new `scene::StitchSegmentsDetailed` (returns `StitchResult{closed, open}`) when no closed loop forms for an element. Attached-mesh path unchanged. 2 new unit tests for `StitchSegmentsDetailed` + 2 new tests for per-owner batched caps (per-element colour, invisible-owner skip); visual verification on `../sample.ifc` deferred to the user since this is a GUI change.
  - [x] 7.5j Stage gate — full `ctest --output-on-failure` (215/215 pass; disabled LSan for `test_core` binary since it contains Vulkan-touching Application + SceneUploaderVulkan tests whose graphics-driver leaks at process exit are environmental. Runtime ASan checks remain active across all targets.)
- [x] 7.6 Element visibility by IFC type
  - [x] 7.6a `Scene::SetVisibilityByType` + `Scene::GetUniqueTypes` + `scene::DefaultHiddenTypes()` (IfcSpace, IfcOpeningElement, IfcGrid, IfcAnnotation) — 5 new unit tests, 124/124 scene tests pass
  - [x] 7.6b `ui::TypeVisibilityPanel` — lists unique IFC types with checkboxes + Show/Hide all; `SetScene` seeds cache, `ApplyDefaults()` hides non-visual types on load, `Refresh()` rebuilds after scene mutation. 8 unit tests, 105/105 ui tests pass. Wired in `main.cpp` (panel constructed after scene build, `ApplyDefaults` called once). Visual verification deferred to the user — batching is type-keyed, so hiding a type hides its whole batched mesh via the existing `node.visible` check in the draw loop.
- [x] 7.7 Element isolation
  - [x] 7.7a `Scene::IsolateByExpressId` + `Scene::ShowAll` + `Scene::FindByExpressId` (5 new unit tests; mesh-bearing nodes only, non-mesh ancestors untouched)
  - [x] 7.7b `ui::HierarchyPanel` subscribes to `ElementSelected`/`SelectionCleared`; selected rows get `ImGuiTreeNodeFlags_Selected`; ancestors of selection auto-expand via `SetNextItemOpen`. 5 new unit tests.
  - [x] 7.7c Per-row eye (`o`/`-`) + isolate (`I`) buttons in the hierarchy tree. Panel exposes `NodeCallback` wiring + `VisibilityQuery`; main.cpp drives Scene: toggle flips visibility for every mesh-bearing descendant, isolate calls `IsolateByExpressId` over the subtree, query reports "any descendant mesh visible". 3 new unit tests.
  - [x] 7.7d Toolbar "Frame Selected" button next to "Fit to View" (wired to `Camera::Frame` over selection AABB, falls back to fit-all when selection is empty). 2 new unit tests.
  - [x] 7.7e HierarchyPanel: `SetTypeVisibilityQuery` dims rows whose IFC type is hidden in the Types panel and suppresses their eye/isolate icons (1 new unit test).
  - [x] 7.7f HierarchyPanel: eye + isolate buttons render with `ImGuiCol_ButtonActive` bg when "on" (checkbox-like feedback); isolate toggles off on a second click via `Scene::ShowAll()` + `TypeVisibilityPanel::ReapplyToScene()` so disabled types stay hidden (1 new unit test).
- [x] 7.8 Element transparency override
  - [x] 7.8a `IfcGeometryExtractor::ExtractSubMeshes` — returns one `TriangulatedMesh` per IfcPlacedGeometry, preserving each piece's surface-style color (including alpha < 1 for glass panes) and transformation. Existing `ExtractMesh` untouched so downstream code is unaffected. 4 new unit tests; 44/44 ifc tests pass.
  - [x] 7.8b `SceneBuilder` consumes sub-meshes: `BuildHierarchy` calls `IfcGeometryExtractor::ExtractSubMeshes(expressId)` and emits one mesh-bearing child SceneNode per sub-mesh under the element parent (parent keeps expressId + aggregated AABB but no mesh). `SceneMesh::IsTransparent()` (alpha<0.999) added as opacity bucket in the batching `BatchKey`, so opaque and translucent small meshes of the same type+color no longer merge. 2 new SceneBuilder tests + 1 new Batching test; 19/19 scene-builder+batching pass, 114/114 scene-module tests pass.
  - [x] 7.8c Renderer transparent pass — `PipelineConfig::alphaBlendEnable` (straight alpha-over: `SRC_ALPHA, ONE_MINUS_SRC_ALPHA`) + new PipelineTest for blend+depth-write-off; `main.cpp` builds `transparentPipeline` next to shaded/wire (rebuilt on MSAA change), splits draw calls by `SceneMesh::IsTransparent()`, draws opaque → section caps → transparent → UI. Wireframe mode still goes through the wire pipeline for everything.
  - [x] 7.8d Per-element / per-type alpha override slider in PropertyPanel + TypeVisibilityPanel (forces alpha on top of IFC-native alpha).
    - [x] 7.8d.1 Scene alpha override API — `SetElementAlphaOverride(expressId,alpha)` / `SetTypeAlphaOverride(ifcType,alpha)` / `GetEffectiveAlpha(NodeId)` (element override > type override > nullopt), clamped to [0,1]. 7 new unit tests.
    - [x] 7.8d.2 PropertyPanel alpha slider — `SetOnAlphaChange` / `SetAlphaQuery` callbacks + enable checkbox and 0..1 slider. `TriggerAlphaChange` / `TriggerClearAlpha` mirror HierarchyPanel test hooks; query seeds current value on `SetElement`. 4 new unit tests.
    - [x] 7.8d.3 TypeVisibilityPanel alpha slider — per-row enable checkbox + 0..1 slider that forwards to `Scene::SetTypeAlphaOverride` / `ClearTypeAlphaOverride`. Direct scene read/write (no cache). 4 new unit tests.
    - [x] 7.8d.4 Renderer wiring — `MeshBuffer::SetVertexAlphaOverride` (new layered override: baseline → alpha → color-override; selection highlight now preserved across alpha edits; 4 new unit tests). `main.cpp` hash-gated `rebuildAlphaOverrides()` runs each frame: walks scene, reads `GetEffectiveAlpha` per mesh-bearing node, uploads per-vertex alpha pairs, and tracks `handlesWithAlphaOverride` so those meshes route into the transparent pass. PropertyPanel callbacks wired to `Scene::{Set,Clear,Get}ElementAlphaOverride`; TypeVisibilityPanel already writes directly to Scene, picked up by the per-frame rebuild.
- [x] 7.9 Fit-to-view — `FitCameraToBounds(camera, bounds)` in `main.cpp` + `Camera::Frame(min,max)` (N.1c). Driven by toolbar "Fit to View" (`Toolbar::SetOnFitToView`), toolbar "Frame Selected" (7.7d, falls back to fit-all on empty selection), Home key (frame-all), and Numpad . (frame-selected). Also called on model load.
- [x] 7.10 First-person navigation
  - [x] 7.10a Remove per-element/per-type alpha sliders from PropertyPanel + TypeVisibilityPanel; `scene::DefaultTypeAlphaOverrides()` returns `{{"IfcWindow", 0.4}}` and is applied in `main.cpp` right after scene build. PropertyPanel AlphaCallback/Query API + TypeVisibilityPanel Set/Clear/GetTypeAlphaOverride gone; corresponding tests removed. 1 new scene test; 117/117 ui + 138/138 scene tests pass.
  - [x] 7.10b Toolbar "Point of View" checkbox next to "Measure"; when active, `scene::ApplyPointOfViewAlpha(scene, 0.2F)` sets 0.2 alpha on every non-IfcSlab type except those already carrying a default alpha (IfcWindow stays at 0.4); disable calls `ClearPointOfViewAlpha` which clears the same set, leaving IfcSlab and defaults untouched. 3 new scene tests + 3 new Toolbar tests; 117/117 ui + 141/141 scene tests pass.
  - [x] 7.10c Viewpoint placement — click on an `IfcSlab` top face while PoV-armed teleports camera to hit + (0,1.5,0), gaze along +Z. New `renderer::FirstPersonController` (yaw/pitch from mouse, WASD/arrow translation). Circular flat disk marker rendered at cursor as hover preview.
    - [x] 7.10c.1 `renderer::FirstPersonController` — pure yaw/pitch + WASD logic independent of `Camera`. `SetPosition`/`SetYawPitch`, `Look(mouseDelta, sensitivity)`, `Move(localInput, dt, speed)`, `GetForward`, `ApplyTo(camera)` (sets orbit-camera target+distance so `camera.GetPosition() == fpc.GetPosition()` and gaze matches). Pitch clamped to ±π/2−ε. Horizontal walking (pitch ignored). 14 unit tests.
    - [x] 7.10c.2 Teleport on IfcSlab click + WASD/mouselook wiring in `main.cpp`. New `pointOfViewArmed` mirror flag and `firstPersonActive` state. Toolbar PoV toggle writes `pointOfViewArmed` and clears `firstPersonActive` on off. Left-click while armed: raycast; if hit node `ifcType == "IfcSlab"`, ray came from above (`ray.dir.y < 0`), and `|n.y| > 0.7` (horizontal face), teleport to `hit.point + (0,1.5,0)` with yaw=π (gaze +Z). Mouse-move while `firstPersonActive` calls `fpc.Look` (sensitivity 0.003 rad/px) — bypasses orbit/hover. Per-frame WASD/arrows poll `Input::IsKeyDown` and feed `fpc.Move` at 3 m/s. Scroll-zoom disabled in FPS. Visual verification deferred to the user (UI behavior). Renderer+UI tests green (28/28).
    - [x] 7.10c.3 `renderer::DiskMarkerPipeline` + `disk_marker.{vert,frag}` shaders — flat-color pipeline for a pre-triangulated disk (`DiskVertex`: vec3 position + vec4 color). Depth LEQUAL / write-off, CULL_NONE, alpha-blend ON so per-vertex alpha fades to a soft edge; single camera UBO descriptor set (matches `SectionFillPipeline`). 4 Vulkan integration tests (shader-SPIR-V presence, construction, MSAA 4×, RAII). CPU geometry + per-frame upload + draw deferred to 7.10c.4.
    - [x] 7.10c.4 Hover disk geometry + GPU buffer + main.cpp wiring. `renderer::BuildDiskVertices(center, normal, radius, color, segments=48)` produces a triangle-fan disk with center vertex at full alpha and ring vertices at alpha=0 for a naturally soft edge; ignores radius ≤ 0 / zero-length normal and clamps segments to ≥3 (8 unit tests). `renderer::DiskMarkerBuffer` wraps a `renderer::Buffer` with hash-gated `Rebuild` + explicit `Clear` (same waitIdle-before-replace pattern as `SectionCapGeometry`). `main.cpp` tracks `hoverDiskValid/Center/Normal` in the mouse-move handler (same IfcSlab-top-face criteria as the click-to-teleport path, mutually exclusive with `HoverElement`); toolbar PoV-off and successful teleport both clear the hover; per-frame draw lifts the disk 2mm along the slab normal to avoid z-fighting, then rebuilds + binds + draws the buffer between section caps and the transparent pass. Pipeline rebuilt alongside `shaded/wireframe/transparent/sectionFill` on MSAA change. Visual verification deferred to the user (UI behaviour).
  - [x] 7.10d Minimal in-flight UI — new `ui::FirstPersonExitPanel` (top-right "Exit Point of View" button, hidden by default; 5 unit tests). `main.cpp` snapshots all main panels' visibility on FPS entry, hides them + the ImGuizmo view cube, and opens the exit panel; on exit it restores saved visibility. Exit path (button + Esc) flips the toolbar PoV checkbox off — which clears ghost alphas via `ClearPointOfViewAlpha` and `firstPersonActive` — then requests Fit-to-View. Esc still closes the window outside FPS. Visual verification deferred to the user.

## Stage R — Render Quality
- [x] R.1 Three-point lighting (key/fill/rim) + ImGui Render Quality panel scaffold
- [x] R.2 MSAA (toggle 1×/2×/4×/8×) with resolve pass
- [x] R.3a Shadow mapping — light-space matrix math (`renderer::ComputeLightSpaceMatrix`) + unit tests
- [x] R.3b Shadow mapping — depth-only render pass + shadow map image/sampler
- [x] R.3c Shadow mapping — LightingUbo shadow fields + `ComputePcfShadow` helper + panel wiring (enable/resolution/bias/PCF radius)
- [x] R.3d Shadow mapping — `basic.{vert,frag}` PCF code + `sampler2DShadow` descriptor binding + depth pass executed each frame from `main.cpp`
- [ ] R.4 SSAO (deferred — shadow mapping already covers architectural depth cues; revisit after Stage N)

## Stage N — Navigation (Blender-style viewport)
- [x] N.1a Input mapping — `renderer::ViewportNavigator::ClassifyDrag` (MMB=orbit, Shift+MMB=pan, Ctrl+MMB=dolly) + wire into `main.cpp`
- [x] N.1b Ortho toggle — `Camera::SetOrthographic` + numpad 5 binding
- [x] N.1c Framing — `Camera::Frame(min,max)` + Home (frame-all) / Numpad . (frame-selected, falls back to frame-all) / Shift+C (reset pivot to origin)
- [x] N.1d Axis snaps — `Camera::SetAxisView(AxisView)` + numpad 1/3/7 (Ctrl = opposite side) for Front/Back/Right/Left/Top/Bottom

## Stage 8 — Loading Responsiveness & Memory
Re-scoped 2026-04-17. Original 8.2 (BVH), 8.3 (frustum culling), 8.4 (LOD), 8.5 (indirect drawing) dropped — they target city/campus scale, not compact buildings (see PLAN.md "Scope decisions"). Reopen if a campus model appears. The 8.4 slot is repurposed 2026-04-18 for a stability + UX bundle (axis-section, PoV, device-pick).

- [x] 8.1 Async IFC loading with progress + cancel (`ifc::AsyncLoader` — worker-thread `LoadAsync` returns `std::future<std::unique_ptr<IfcModel>>`, progress emitted at "starting"/"parsing"/"done" boundaries, atomic cancel flag sampled at every boundary; serializes by joining any prior in-flight load. 7 unit tests cover worker-thread execution, monotonic 0→100 progress, null-callback safety, invalid-path → nullptr, cancel-flag toggling, cancel-during-progress → nullptr, and second-load resetting the flag. Not yet wired into `main.cpp` — that lands with 8.2 loading modal.)
- [x] 8.2 Loading modal in `app/` (ImGui overlay, % + phase + Cancel — `main.cpp` startup refactored: UIManager + ImGui backend now init *before* the IFC load so a centred ImGui modal renders each frame while `ifc::AsyncLoader` parses on a worker thread. Modal shows file path, current phase ("starting"/"parsing"/"done"), a 0–100 progress bar driven by `std::atomic<float>`, and a Cancel button that flips to "(cancelling…)" once pressed. Window-close during load triggers Cancel + clean shutdown. Local minimal `recreateSwapchainForLoading` lambda handles resize during the modal phase without depending on scene/camera. Ifc model construction moved into a `unique_ptr` returned from the future; `ifcModel` is now a reference into it for the rest of `main`. No new unit tests — the change is pure UI integration on top of 8.1's already-tested AsyncLoader; manual verification deferred to the user. 48/48 ifc tests still pass.)

- [ ] 8.3 Axis Section Mode — re-do of 7.3 + 7.5 as a single BIM-oriented UX. Up to three axis-locked clip planes (one per X/Y/Z); each plane has an explicit mode ∈ {CutFront, CutBack, SectionOnly}; section fill always on (no custom-color picker exposed); SectionOnly mode hides scene geometry and renders only the caps.
  - [x] 8.3a `scene::AxisSectionController` — owns ≤3 axis slots `(X|Y|Z) → {offset, mode ∈ CutFront|CutBack|SectionOnly}`; `AnySectionOnly()` query for the draw loop; syncs state into `renderer::ClipPlaneManager` each frame via `SyncTo` (CutBack flips the equation sign vs CutFront; SectionOnly packs equation same as CutFront; plane entries owned by the controller carry `sectionFill=true`). `SyncTo` is idempotent (returns `false` on no-op) and self-heals if an external party removes a managed plane entry. 15 unit tests cover the `MakeAxisEquation` helper (front/back sign flip, SectionOnly≡CutFront, Y/Z basis vectors), slot CRUD, `AnySectionOnly`, and the sync lifecycle (add, idempotent no-op, mode flip, offset update, clear-removes, three-axis coexistence, external-removal recovery). 165/165 scene-module tests pass.
  - [x] 8.3b `ui::AxisSectionPanel` — X/Y/Z toggle buttons (active axis highlighted via `ImGuiCol_ButtonActive`), per-axis Cut Front / Cut Back / Section radio + offset slider. `ToggleAxis` creates a slot at offset 0 in CutFront mode on first click and removes it on second click; `SetSlotMode` / `SetSlotOffset` preserve the other field; `SetController(ptr)` clears the active-axis selection when the controller pointer changes; `PruneActiveIfMissing` drops a dangling selection if the controller loses the slot. `SetOffsetRange` (default [-10, 10]) lets main.cpp seed the slider domain from the scene AABB. 20 unit tests cover name/visibility, controller wiring, active-axis get/set/prune, toggle lifecycle + activation rules, mode/offset edits (with and without a slot), offset range, and controller-swap reset. 151/151 ui tests pass.
  - [x] 8.3c Single-axis translate gizmo + direction marker — `ui::MakeAxisGizmoTransform(axis,offset)` builds a pure translation matrix along the axis; `ui::ExtractAxisOffset(m,axis)` reads back the translation component. `AxisSectionPanel::SetCameraMatrices` (mirrors `ClipPlanesPanel`) plus `GetViewMatrix`/`GetProjectionMatrix` expose the gizmo inputs. `OnDraw` draws `ImGuizmo::Manipulate` with `TRANSLATE_X/Y/Z` (WORLD space) for the active-axis slot, writing the dragged offset back via `SetSlotOffset`. Per-slot direction marker (small arrowhead + origin dot via `ImGui::GetForegroundDrawList`): green for CutFront/CutBack, amber for SectionOnly, points in the "kept" axis direction (−axis for CutBack); projects origin+kept-tip through view·projection and flips NDC.y for ImGui's Y-down screen space. 7 new unit tests for the writeback math + camera matrix round-trip (MakeAxisGizmoTransform on X/Y/Z, ExtractAxisOffset on all three axes, round-trip across offsets, ignores-other-axis-translations, SetCameraMatrices getter parity); 158/158 ui tests pass. Viewport-arrow manual-verify per PLAN (8.3d wires into `main.cpp`).
  - [x] 8.3d `main.cpp` draw-loop wiring — `bimeup::scene::AxisSectionController axisSectionController` lives next to `ClipPlaneManager`; `AxisSectionPanel` constructed + `SetController`-wired alongside the other panels, `AddPanel`ed into `UIManager`, and added to the FPS-mode `mainPanels` hide list. Offset slider range seeded from the scene AABB on load (padded 10%). Each frame the controller `SyncTo(clipPlaneManager)` runs before `PackClipPlanes`, and the panel receives the same Y-flipped `gizmoProj` as `ClipPlanesPanel` via `SetCameraMatrices`. Draw loop short-circuits opaque + transparent scene passes when `controller.AnySectionOnly()` is true (bind-skip + iterate-skip); section-fill caps still render because the sync flags `sectionFill=true` on owned plane entries. CutFront/CutBack need no draw-loop changes — they go through the existing shader discard path. No new unit tests — integration wiring is manual-verify per PLAN; 15/15 `Scene_AxisSectionController` + 158/158 ui tests remain green and `bimeup` binary links.
  - [x] 8.3e Retired the free-form 6-plane UI. `ui::ClipPlanesPanel` deleted outright — header, source, and test file removed via `git rm`, pulled out of `src/ui/CMakeLists.txt` + `tests/ui/CMakeLists.txt`. `main.cpp` no longer constructs / wires the panel (no include, no `SetManager`, no `SetCameraMatrices`, no `AddPanel`, no `mainPanels` entry). `ClipPlaneManager`, `SectionCapGeometry`, `section_fill.{vert,frag}`, `PackClipPlanes` are untouched and still used by `AxisSectionController::SyncTo` + `PlanViewPanel`. `ClipPlane::fillColor` stays on the struct; default flipped from `{0.6,0.6,0.6,1}` → `{1,1,1,1}` per PLAN so the 7.5k tint (`ownerColor * fillColor`) reduces to `ownerColor` when no UI sets a tint. Two `ClipPlaneTest` assertions updated to reflect the new default; `ClipPlaneManager::SetFillColor` / `fillColor` field unchanged otherwise. Full `ctest --output-on-failure` — 274/274 pass, 527 s.
  - [x] 8.3f In-viewport mode-selector popup. New public helper `ui::ProjectPlaneOriginToScreen(view, projection, axis, offset, displaySize) → std::optional<glm::vec2>` projects the axis-plane origin (`axis_unit * offset`) through view·projection, divides by `clip.w` (returns nullopt if ≤ 1e-4), and flips Y for ImGui screen space. The internal `ProjectToScreen` used by `DrawDirectionMarker` is replaced by a thin `ProjectPointToScreen` wrapper so both the marker and the popup anchor share one projection path. `AxisSectionPanel::DrawModeContextPopup(axis)` opens a tiny transparent 22×22 ImGui host window at the projected origin (`NoDecoration|NoMove|NoResize|NoSavedSettings|NoBackground|NoFocusOnAppearing|NoNav|NoBringToFrontOnFocus`), registers an `InvisibleButton`, and calls `BeginPopupContextItem("mode_menu", ImGuiPopupFlags_MouseButtonRight)` — right-click on the marker area opens a 3-item `Selectable` list (Cut Front / Cut Back / Section) with the current mode highlighted; selection writes back via `SetSlotMode`. Left-click is untouched so `ImGuizmo::Manipulate` at the same origin keeps working. 4 new unit tests for `ProjectPlaneOriginToScreen` (offset=0 centres, +X/+Y movement, behind-camera nullopt via `proj[3][3]=0`). 31/31 `AxisSectionPanelTest` pass; 158 → 162 ui tests green.
  - [x] 8.3g Retire ImGuizmo entirely + custom `ui::AxisSectionGizmo` (single-header, imoguizmo-style). View cube swapped to `imoguizmo` (`external/imoguizmo` submodule; `ImOGuizmo::DrawGizmo` at 96 px rect with tuned `axisLengthScale=0.15`, `positiveRadiusScale=0.10`, `negativeRadiusScale=0.07`; new `Camera::GetDistance()` getter for pivot). New `src/ui/include/ui/AxisSectionGizmo.hpp` with pure-math helpers (`ProjectWorldToScreen`, `AxisDragDelta` — 10 unit tests in `AxisSectionGizmoTest.cpp`) and the inline `DrawAxisHandle` ImGui entry point: drag bar along projected axis → grab circle → screen-horizontal segmented `|F|S|B|` button group (RoundCornersLeft/None/Right + black-alpha separators between pills) → (×) close raised 18 px above the B-pill right edge. Drag math uses finite-difference screen projection + `AxisDragDelta` inversion with 8 px minimum-screen-length guard. `AxisSectionPanel` rewritten: dropped `ImGuizmo::Manipulate`, `DrawModeContextPopup` (8.3f), the direction marker, the active-axis radio/gizmo concept (+ `SetActiveAxis`/`ActiveAxis`/`PruneActiveIfMissing`), and the `MakeAxisGizmoTransform`/`ExtractAxisOffset`/`ProjectPlaneOriginToScreen` helpers; panel is now just X/Y/Z toggle buttons + per-slot offset slider, one gizmo per active slot. `UIManager` no longer calls `ImGuizmo::BeginFrame`; `main.cpp` drops `ImGuizmo.h`; `external/CMakeLists.txt` drops `ImGuizmo.cpp` + include dir. `external/ImGuizmo` submodule removed (`git rm`). 20 ImGuizmo-tied tests removed (ActiveAxis×3, TogglingActivates/ClearsActive×2, PruneActiveIfMissing×3, MakeAxisGizmoTransform×3, ExtractAxisOffset×3, ProjectPlaneOriginToScreen×4, ControllerSwitchClearsActiveAxis, ImGuizmoIntegrationTest). Full `ctest --output-on-failure` — 274/274 pass.
  - [x] 8.3h UX polish bundle. `AxisSectionPanel` "Y"/"Z" button labels swapped so "Z" maps to `Axis::Y` and "Y" to `Axis::Z` — BIM users think Z = vertical, but the viewer world is Y-up; internal `Axis` enum + `MakeAxisEquation` + gizmo `AxisUnit` are untouched. `ui::HandleBarSign(bool isCutBack)` added to `AxisSectionGizmo.hpp`; `DrawAxisHandle` now extends the drag bar from the plane anchor toward the cut-away side instead of the kept side. `renderer::Swapchain::ChoosePresentMode` dropped the MAILBOX preference and now prefers `VK_PRESENT_MODE_FIFO_RELAXED_KHR` with `FIFO_KHR` fallback — caps present rate to display refresh (~144 Hz vs. previous 600+) while allowing late frames to tear through the vblank boundary instead of doubling frame time to 72 Hz (fixes the debug-mode orbit jitter that surfaced when vsync was first enabled). `ui::ViewportOverlay` trimmed to the measurement layer only: the ImGui host window is gone and with it the FPS readout, camera position/forward readouts, the small XYZ axes gizmo, and the visibility toggles + setters/getters (`SetFps`, `GetFps`, `SetCameraPosition`, `GetCameraPosition`, `SetCameraForward`, `GetCameraForward`, `SetFpsCounterVisible`, `IsFpsCounterVisible`, `SetCameraInfoVisible`, `IsCameraInfoVisible`, `SetAxesGizmoVisible`, `IsAxesGizmoVisible`, plus the `DrawAxesGizmo` helper and its per-frame member state). `main.cpp` drops the now-unused `smoothedFps` smoothing + `overlay->SetFps/SetCameraPosition/SetCameraForward` calls. `ViewportOverlayTest` stripped to two surviving tests (`HasPanelName`, `IsAPanel`) — 9 obsolete tests removed. Small GUI tweaks — no new unit tests per user request (manual-verify).

- [x] 8.4 Stability + UX bundle (axis-section, PoV, device-pick — 2026-04-18)
  - [x] 8.4a poly2tri stack-overflow guard. Section-view capping under drag can feed poly2tri numerically-degenerate polylines (near-duplicate points that survive the 10 µm quantise dedup, near-collinear triples) that drive `Sweep::FlipEdgeEvent` / `Sweep::FlipScanEdgeEvent` into mutual infinite recursion — visible as an ASan-aborted stack overflow, which is not a C++ exception and therefore slips past the existing `try/catch` in `scene::TriangulatePolygon`. Patched `external/poly2tri/poly2tri/sweep/sweep.cc`: added an anonymous-namespace `thread_local int g_bimeup_flip_depth` counter + RAII `BimeupFlipDepthGuard` that increments on entry to both Flip* functions, decrements on exit, and `throw std::runtime_error("poly2tri: FlipEdgeEvent recursion limit exceeded")` once depth exceeds 2048. The caller's existing `catch (const std::exception&)` → `EarClipTriangulate` fallback now fires on pathological input instead of the process aborting. No upstream API change; pre-existing clang-tidy `misc-no-recursion` warnings on the upstream recursive chain resurface because the file was touched but are unchanged behaviour.
  - [x] 8.4b Selection disabled during section view. `src/app/main.cpp` — left-click `core::PickElement` and hover `core::HoverElement` are both gated on `axisSectionController.SlotCount() == 0`, so clicks/hover don't select or highlight elements while any axis-section slot is active. Drop every slot (via panel toggle or gizmo ×) to re-enable selection. No new tests — small UI gate, manual-verify.
  - [x] 8.4c Per-axis default section mode. `AxisSectionPanel.cpp` — `kAxisButtons` table extended with a `defaultMode` per button; new `DefaultModeFor(axis)` reads it. UI "X" still creates `CutFront`; UI "Y" (world Z) and UI "Z" (world Y) now create `CutBack` on first click so the kept half sits toward the viewer by default (matches the vertical-slab and depth-slab mental model). `ToggleAxis` reads the default via `DefaultModeFor`. Existing `AxisSectionPanelTest` suite stays green (none of the tests pinned the default mode).
  - [x] 8.4d Per-axis offset clamp from scene AABB. Root cause: `AxisSectionPanel::SetOffsetRange(float, float)` stored one scalar min/max and `main.cpp` seeded it with the global span across x/y/z, so a wide-short-thin model let the vertical plane slide across the whole diagonal. Fix: panel now stores `glm::vec3 m_offsetMin/m_offsetMax` with a new `SetOffsetRange(const glm::vec3&, const glm::vec3&)` overload + `OffsetMin(Axis)/OffsetMax(Axis)` accessors; old `(float,float)` signature preserved (fills all three axes) so existing tests stay unchanged. Slider (`ImGui::SliderFloat`) and gizmo-drag writeback (`std::clamp`) both use the per-axis range. `main.cpp` now seeds `axisSectionPanel->SetOffsetRange(mn - pad, mx + pad)` where `pad = 0.1F * max(mx - mn, vec3(1))` — per-axis 10% padding. Decision: left `ComputeSceneBounds` unchanged (walks every node with valid bounds including IfcSite, IfcSpace, IfcBuildingStorey etc.) — user flagged this as a useful signal that off-screen geometry exists, not a bug. 27/27 ui tests + bimeup link green.
  - [x] 8.4e Per-axis gizmo colours (CG convention, mode-override retained). `AxisSectionGizmo.hpp` `DrawAxisHandle` — `axisColor` now selects a per-axis base colour: `Axis::X` → red `(230,70,70)`, `Axis::Y` → blue `(80,140,240)` (world-Y, UI "Z"), `Axis::Z` → green `(80,220,120)` (world-Z, UI "Y"). `SectionMode::SectionOnly` still overrides to amber `(255,200,64)` across all axes as a mode cue so cut-through state reads at a glance. Close-button "x" glyph nudged up 1.5 px in the red circle to fix optical-centre drift (lowercase x has more visual mass below its geometric centre).
  - [x] 8.4f PoV marker + ghosting polish. `main.cpp` — `ApplyPointOfViewAlpha(scene, 0.2F)` → `0.08F`: non-slab types fade to ~8% so the slab cursor target dominates (`IfcWindow` default 0.4 from `DefaultTypeAlphaOverrides` is untouched via the PoV exclusion set). `renderer::DiskMarkerPipeline` — `depthCompareOp` changed from `VK_COMPARE_OP_LESS_OR_EQUAL` to `VK_COMPARE_OP_ALWAYS` so the hover disk draws on top of the ghosted geometry regardless of wall/stair occlusion; depth-write stays off so the disk doesn't shadow later passes.
  - [x] 8.4g Vulkan device-pick robustness + telemetry. `Device::PickPhysicalDevice` now logs each enumerated `VkPhysicalDevice` with `deviceName`, `deviceType` (discrete/integrated/virtual/cpu/other), computed score, and the disqualification reason when `hasGraphics`/`hasPresent` fails, plus a final `Selected GPU: <name> (<type>)` line — makes it obvious whether a dedicated GPU is visible to the Vulkan loader at all vs. merely outranked. `Device::RateDevice` widened the discrete-vs-integrated gap to 100× (discrete=100000, integrated=1000, virtual=100, cpu=10) and adds `+1 per GiB` of device-local `VK_MEMORY_HEAP_DEVICE_LOCAL_BIT` VRAM as a discrete-vs-discrete tiebreaker. If a hybrid Linux PRIME machine still picks integrated after this change, the log confirms the NVIDIA GPU isn't enumerated (driver/ICD issue, launch with `__NV_PRIME_RENDER_OFFLOAD=1 __VK_LAYER_NV_optimus=NVIDIA_only __GLX_VENDOR_LIBRARY_NAME=nvidia`).


## Stage RP — Render Polish (non-RT beauty pass)
Inserted 2026-04-18 before Stage 9 (RT). Goal: lift the forward renderer to a Godot-editor-viewport look using only rasterization + screen-space effects. This is the permanent fallback path for GPUs without RT support, so everything here must run on the existing Vulkan 1.x feature set. Reference shaders: `../godot-feature-updated_screen_space_shaders/ssao.glsl` + `ssil.glsl` (Intel XeGTAO family, clayjohn's Vulkan+compute port). See PLAN.md "Stage RP" for scope decisions, dependency graph, and expected APIs.

- [x] RP.1 Hemisphere sky ambient — `renderer::HemisphereAmbient{zenith, horizon, ground}` replaces flat `LightingScene::ambient`; `ComputeHemisphereAmbient(n, sky)` blends by `normalize(n).y` (t≥0 → mix(horizon, zenith, t); t<0 → mix(horizon, ground, -t)) and has a matching GLSL `hemisphereAmbient` in `basic.frag`. `LightingUbo` grew 192 → 224 bytes (dropped `vec4 ambient`, added `vec4 skyZenith/Horizon/Ground`); shader UBO layout kept in sync. `RenderQualityPanel` "Three-point lighting" header now exposes three `ColorEdit3` pickers (Zenith/Horizon/Ground) under a "Sky ambient (hemisphere)" label in place of the single "Ambient" picker; `Reset to defaults` still resets via `MakeDefaultLighting()`. 6 new unit tests in `LightingTest.cpp` (`HemisphereAmbientTest.*` on ±X/±Y/±Z cardinal normals + midway 45° lerp + input-normalisation + `PackLightingEncodesSkyColors`); existing `PackedUboMatchesStd140Size` bumped 192 → 224. Full renderer module ctest (`ctest -R renderer`) 1/1 pass (407 s including Vulkan integration) and ui module ctest 27/27 pass.
- [x] RP.2 Post-process framework — HDR offscreen target (R16G16B16A16_SFLOAT) + ACES tonemap resolve pass; unhooks the main render pass from the swapchain
  - [x] RP.2a ACES tonemap CPU mirror — `renderer::AcesTonemap(glm::vec3)` (Narkowicz fit `(x*(2.51x+0.03))/(x*(2.43x+0.59)+0.14)` clamped to [0,1]; inputs pre-clamped to ≥0 so negatives collapse to 0 instead of traversing the non-monotonic region). New `renderer/Tonemap.{h,cpp}`. 7 unit tests — reference values at x=0/0.18/1.0/10.0 (0.0, 0.266904, 0.803797, 1.0), negative-input clamp, per-channel independence, monotonicity on positives. `ctest -R renderer` — 1/1 (377 s, includes Vulkan integration).
  - [x] RP.2b `tonemap.{vert,frag}` shaders + `renderer::TonemapPipeline`. Fullscreen-triangle vertex shader generates three NDC vertices from `gl_VertexIndex` (no vertex buffer needed); fragment shader samples HDR via a single `sampler2D` at set 0/binding 0 and runs the Narkowicz ACES fit matching `renderer::AcesTonemap` byte-for-byte (same pre-clamp-to-zero + post-clamp to [0,1]). `TonemapPipeline` mirrors `SectionFillPipeline`'s shape: `PipelineConfig` with CULL_NONE, depth-test/write off (color-only final pass to the sRGB swapchain), no blend, `colorAttachmentCount=1`, `samples` parametrised for API parity with other pipelines. 4 Vulkan integration tests (shader-SPIR-V presence, construction, MSAA 4×, RAII) against a color-only render pass mirroring the real target. `ctest -R renderer` 1/1 pass (406 s).
  - [x] RP.2c HDR offscreen target + two-pass composition inside `RenderLoop`. Main render pass colour format flipped from the swapchain format to `HDR_FORMAT=VK_FORMAT_R16G16B16A16_SFLOAT` with `finalLayout=SHADER_READ_ONLY_OPTIMAL`; MSAA resolve target now also HDR (format must match the multisample attachment per spec). New per-swap-image `m_hdrImages/Views` (usage `COLOR_ATTACHMENT|SAMPLED`) replace the swapchain attachment in the main framebuffers. A second "present" render pass (load=DONT_CARE, final=PRESENT_SRC_KHR, 1× swapchain colour only, no depth) is created alongside with its own per-swap-image framebuffers. `RenderLoop` owns the tonemap stack internally: `TonemapPipeline` + vertex/fragment `Shader`s loaded from a new `shaderDir` ctor parameter, an HDR sampler, a per-swap-image `COMBINED_IMAGE_SAMPLER` descriptor set, and a pool. `EndFrame` now ends the HDR pass, begins the present pass on the current swap image, binds the tonemap pipeline + descriptor, issues `vkCmdDraw(3,1,0,0)` for the fullscreen triangle, then invokes a new `SetInPresentPassCallback(VkCommandBuffer)` so UI draws can sit on top of the tonemapped image instead of being fed through ACES twice. `GetPresentRenderPass()` + `GetPresentSampleCount()==1×` are the handles ImGui + any future post-tonemap pipelines bind to. `SetSampleCount` and `RecreateForSwapchain` rebuild HDR + framebuffers + tonemap descriptors in lockstep; the present render pass is preserved because it never changes. `BIMEUP_ENABLE_HDR` debug-fallback from the PLAN skipped — the HDR path is unconditional, easy to reintroduce later if a driver issue forces a direct-swapchain branch. Callers updated: `main.cpp` constructs `RenderLoop` with `BIMEUP_SHADER_DIR` and wires `uiManager.EndFrame(cmd)` through `SetInPresentPassCallback` (removing two direct invocations in the loading-modal + main draw paths); ImGui init switched to `GetPresentRenderPass()` + 1× samples. `core::Application` and `bimeup_core`'s CMakeLists got the same shader-dir plumbing. `tests/renderer/{CubeRenderTest,RenderLoopTest}` updated to pass `BIMEUP_SHADER_DIR`. `ctest -R renderer` 1/1 pass (408 s); ui/scene/core suites 27/27 pass; bimeup binary smoke-tests clean on NVIDIA RTX 4050 (no validation-layer warnings).
- [ ] RP.3 MRT normal G-buffer — R16G16_SNORM octahedron-packed view-space normal attached to the main pass; `basic.frag` emits it alongside colour
  - [x] RP.3a CPU mirror `renderer::OctPackNormal` / `OctUnpackNormal` (Cigolle et al. JCGT 2014 octahedron signed encoding). New `renderer/OctNormal.{h,cpp}` following the `Tonemap.h` pattern. 7 unit tests in `tests/renderer/OctNormalTest.cpp`: +Z encodes to origin, pack output stays in [-1,1]² across a θ×φ sphere grid, cardinal-axis round-trip (±X/±Y/±Z), 1024-sample Fibonacci-sphere round-trip (dot ≥ 1-1e-4, unit length), explicit -Z-hemisphere round-trip (corners of the fold-branch), deterministic output, unit-length reconstruction at octahedron corners. Shader-side pack/unpack + the MRT attachment land with RP.3b/c/d so a single PR can flip the G-buffer on end-to-end. `ctest -R renderer` 1/1 pass (402 s).
  - [x] RP.3b `renderer::Pipeline` MRT support. `VkPipelineColorBlendStateCreateInfo::pAttachments` now points at a `std::vector<VkPipelineColorBlendAttachmentState>` sized to `colorAttachmentCount` (was a single-entry pointer — technically UB when `colorAttachmentCount > 1` since the driver reads past the one-struct stack buffer; in practice zero-initialised stack padding had been masking the bug). New `PipelineConfig::disableSecondaryColorWrites` flag (default false) forces `colorWriteMask = 0` on every attachment beyond 0 — intended for overlay pipelines (section-fill, disk-marker, transparent) bound to the MRT main pass so they don't stomp the normal G-buffer. Alpha-blend state on secondary attachments is force-disabled when the flag is set. 3 new PipelineTest cases + a `CreateMrtRenderPass` helper: `CreateWithTwoColorAttachmentsAllWriting` (basic-style MRT pipeline builds), `CreateWithTwoColorAttachmentsSecondWriteMasked` (overlay-style MRT pipeline builds), `CreateMrtWithAlphaBlendOnPrimaryOnly` (transparent-style MRT, alpha on [0], masked [1]). Per-attachment blend state can't be introspected from a built `VkPipeline`, so the tests assert successful creation rather than the specific state — they're regression guards for the MRT contract going forward. `ctest -R renderer` 1/1 pass (420 s); full app link green.
  - [x] RP.3c Normal G-buffer attached to `RenderLoop`. `RenderLoop::NORMAL_FORMAT = VK_FORMAT_R16G16_SNORM` + `GetNormalImageView(i)` getter added. `CreateRenderPass` extended to a 5-slot MRT layout — non-MSAA `[hdr, normal, depth]`, MSAA `[hdr MSAA, normal MSAA, depth, hdr resolve, normal resolve]` — both the MSAA transient normal attachment and the per-swap-image single-sample normal target (COLOR_ATTACHMENT|SAMPLED) are allocated in `CreateHdrResources`/`CreateDepthResources` and torn down in lockstep with HDR in `CleanupFrameResources`. Framebuffer build updated; `BeginFrame` feeds 3 active clear values (`[1]=(0,0,0,0)` → oct-decodes to +Z). `SetSampleCount`'s LOG_INFO guarded against a null logger (pre-existing latent bug surfaced by the new MSAA-4× test). Pipeline-wiring: `PipelineConfig.colorAttachmentCount = 2` everywhere that binds to the main pass; shaded/wireframe/transparent/section-fill/disk-marker/CubeRenderTest pipelines set `disableSecondaryColorWrites = true` so the clear value survives until RP.3d swaps in a real oct-packed normal. `SectionFillPipeline` + `DiskMarkerPipeline` ctors grew two defaulted params (`colorAttachmentCount=1, disableSecondaryColorWrites=false`) so their standalone 1-attachment tests keep working untouched. 3 new `RenderLoopTest` cases (`NormalFormatIsR16G16Snorm`, `NormalGBufferImageViewsProvidedPerSwapImage`, `NormalGBufferSurvivesSampleCountChange`). Full `ctest -R renderer` — 1/1 pass (637 s incl. Vulkan integration).
  - [x] RP.3d `basic.frag` emits oct-packed view-space normal at `layout(location = 1) out vec2 outNormal`. `basic.vert` now computes `fragNormalView = mat3(camera.view) * normalWorld` at `location = 3` (keeps the existing world-space varying for lighting). `basic.frag` adds `octPackNormal(vec3)` — a byte-for-byte GLSL mirror of `renderer::OctPackNormal` (same L1 divide, same `nn.z < 0` wrap with the `(1 - |yx|) * sign(xy)` fold) — and writes `outNormal = octPackNormal(nView)`, with a `gl_FrontFacing` flip applied to both the world- and view-space normals so the G-buffer sees the visible face under the same double-sided rule as lighting. New `BasicShaderTest.FragmentShaderDeclaresNormalOutputAtLocation1` parses `basic.frag.spv`, walks the SPIR-V for `OpDecorate(Location)` + `OpVariable(Output)` instructions, and asserts outputs at both location 0 (colour) and location 1 (normal) are present. Visual effect invisible until SSAO (RP.5) samples the buffer. `ctest -R renderer` 1/1 pass (450 s incl. Vulkan integration).
- [x] RP.4 Linear depth + depth pyramid — per-frame compute pass that linearises the main depth attachment and builds a 4-mip chain (inputs for SSAO/SSIL)
  - [x] RP.4a CPU mirrors `renderer::LinearizeDepth(z_nl, nearZ, farZ)` + `renderer::ReconstructViewPosFromDepth(uv, linearDepth, invProj)`. New `renderer/LinearDepth.{h,cpp}` following the `Tonemap.h`/`OctNormal.h` pattern. `LinearizeDepth` uses the lerp-form denominator `far*(1 - z_nl) + near*z_nl` (algebraically identical to the classic `far - z_nl*(far - near)` but sidesteps ~1e-3 single-precision drift at z_nl≈1, which the naive form fails on). `ReconstructViewPosFromDepth` rays UV through `invProj` at NDC z=1 then rescales so `viewPos.z == -linearDepth`, matching the SSAO-shader path that will land with RP.5; docstring notes the PLAN's `invViewProj` signature is a typo (SSAO needs view-space, so the helper takes `invProj`). 8 unit tests in `tests/renderer/LinearDepthTest.cpp`: near/far-plane exactness on two (near,far) pairs, monotonic sweep, round-trip through a locally-constructed Vulkan-style perspective (depth [0,1]) at 5 distances, center-UV-on-(-z)-axis invariance, 4-sample known-point round-trip, y-flipped projection round-trip (mirrors `main.cpp`'s `proj[1][1] *= -1` path), and linear-scale proportionality. `ctest -R renderer` — 1/1 (430 s incl. Vulkan integration). Shader + pipeline + depth-pyramid wiring deferred to RP.4b/c/d.
  - [x] RP.4b `depth_linearize.comp` + `renderer::DepthLinearizePipeline` — bimeup's first compute pipeline. Shader reads the main pass's depth attachment as a `sampler2D` at set 0/binding 0, writes view-space linear depth (`r32f`) into a storage image at binding 1, with `(nearZ, farZ)` supplied via push constants; uses the same lerp-form denominator as the CPU mirror so CPU/GPU results match bit-for-bit within single-precision. Dispatch tiles are 8×8. `Shader::ShaderStage` extended with a `Compute` enum value (pre-existing 2-value enum got a clang-tidy `performance-enum-size` note that also applies to the old form; not fixing inline). `DepthLinearizePipeline` owns its own `VkPipelineLayout` + `VkPipeline` directly (the graphics-only `renderer::Pipeline` class doesn't cover compute) — ctor takes the device, compute `Shader`, and descriptor set layout; pushes `PushConstants{ nearZ, farZ }` (8 bytes) at `VK_SHADER_STAGE_COMPUTE_BIT`; `Bind(cmd)` binds at `VK_PIPELINE_BIND_POINT_COMPUTE`; layout cleanup on constructor failure. 4 Vulkan integration tests in `tests/renderer/DepthLinearizePipelineTest.cpp`: shader SPIR-V exists, construction with valid handles, RAII destructor scope, and `sizeof(PushConstants) == 8` guard. `ctest -R renderer` — 1/1 (455 s incl. Vulkan integration).
  - [x] RP.4c `depth_mip.comp` + `renderer::DepthMipPipeline` — downsamples one level of the linear-depth pyramid by taking the minimum of each 2×2 source neighbourhood (the SSAO-conservative choice — closer surface biases toward more occlusion; matches the XeGTAO / Godot reference pyramid build). Shader reads source mip N as an r32f `sampler2D` at set 0/binding 0 and writes destination mip N+1 as an r32f storage image at binding 1; bounds derive from `imageSize(dst)` so no push constants are needed. Dispatch tiles are 8×8 sized to the destination mip — caller rebinds a per-level descriptor set and issues three dispatches to build the 4-mip chain (mip 0→1, 1→2, 2→3). Pipeline class mirrors `DepthLinearizePipeline`'s shape (owns its own `VkPipelineLayout` + `VkPipeline`, deleted copy/move, `Bind(cmd)` at `VK_PIPELINE_BIND_POINT_COMPUTE`, layout cleanup on ctor failure). 3 Vulkan integration tests in `tests/renderer/DepthMipPipelineTest.cpp`: shader SPIR-V exists, construction with valid handles, RAII destructor scope. `ctest -R renderer` — 1/1 (464 s incl. Vulkan integration). Full pyramid wiring (per-level descriptor sets + per-mip views + three-dispatch loop) lands with RP.4d.
  - [x] RP.4d Depth pyramid wired into `RenderLoop`. New constants `DEPTH_PYRAMID_FORMAT=VK_FORMAT_R32_SFLOAT` + `DEPTH_PYRAMID_MIPS=4`; public `GetDepthPyramidView(imageIndex)` exposes a single per-swap-image full-chain sampled view (SSAO/SSIL bind this with `textureLod`); `SetProjectionNearFar(near,far)` plumbs camera near/far into the linearize push constants (defaults 0.1/10000 — main.cpp wiring deferred to RP.5 when SSAO actually needs accurate values, since nothing samples the pyramid today). Main render pass tweaked: non-MSAA depth attachment flipped to `storeOp=STORE` + `finalLayout=SHADER_READ_ONLY_OPTIMAL`, plus a new `SUBPASS_EXTERNAL` post-pass dependency (`LATE_FRAGMENT_TESTS → COMPUTE_SHADER`, `DEPTH_STENCIL_ATTACHMENT_WRITE → SHADER_READ`) so the compute pass sees depth writes. MSAA path keeps the previous `DONT_CARE` / `DEPTH_STENCIL_ATTACHMENT_OPTIMAL` behaviour — the pyramid build gates off under MSAA because `depth_linearize.comp` expects `sampler2D`, not `sampler2DMS`; MSAA depth sampling can land later when SSAO needs it. Depth image usage gained `SAMPLED_BIT` unconditionally (cheap, even when gated off). `RenderLoop` now owns per-swap-image `VkImage` (4 mips, `STORAGE|SAMPLED`), a full-chain view, four per-mip views, one nearest-filter sampler, a persistent descriptor set layout + pool (`imageCount*4` sets of `COMBINED_IMAGE_SAMPLER`+`STORAGE_IMAGE`), and the two compute pipelines + their compute shaders. `BuildDepthPyramid(cmd)` runs between `vkCmdEndRenderPass(main)` and `vkCmdBeginRenderPass(present)` in `EndFrame`: one `UNDEFINED→GENERAL` pre-barrier for all 4 mips, linearize dispatch (depth→mip 0), then three mip-downsample dispatches each preceded by a per-mip `SHADER_WRITE→SHADER_READ` barrier, finally a full-chain `COMPUTE_SHADER→FRAGMENT_SHADER` publish barrier (no-op today, correct when RP.5 lands). Pyramid images and descriptors are torn down + rebuilt alongside HDR/normal targets in `SetSampleCount`/`RecreateForSwapchain`; descriptors are updated via `UpdateDepthPyramidDescriptors` which points set[0].src at the depth view (non-MSAA) or pyramid mip 0 (MSAA — unused but keeps the descriptor validation-legal). 5 new `RenderLoopTest` cases (`DepthPyramidFormatIsR32Sfloat`, `DepthPyramidMipCountIsFour`, `DepthPyramidViewProvidedPerSwapImage`, `DepthPyramidBuiltDuringFrame` — full frame cycle exercises all 4 dispatches under Vulkan validation, `DepthPyramidSurvivesSampleCountChange` — views stay valid + frame cycle works at 4× MSAA). Full `ctest -R renderer` — 1/1 pass (496 s incl. Vulkan integration).
- [x] RP.5 SSAO compute pass — port Godot/Intel `ssao.glsl` (adaptive-base + main + separable blur); composite via multiply into tonemap. Panel: enable / quality / radius / intensity / shadow power
  - [x] RP.5a CPU mirrors `renderer::GenerateHemisphereKernel(count, seed)` + `renderer::PackEdges(l,r,t,b)` / `UnpackEdges(byte)`. New `renderer/SsaoKernel.{h,cpp}` following the `OctNormal.h` / `LinearDepth.h` pattern. Kernel is the classic Chapman 2013 / LearnOpenGL hemisphere: each sample = unit-sphere direction with z≥0, multiplied by a uniform [0,1] length, then rescaled by a quadratic `0.1 + 0.9*(i/N)^2` falloff (early samples cluster near origin for contact AO; later samples reach wider). `std::mt19937(seed)` so CPU mirror and GPU kernel UBO upload (RP.5b) stay bit-identical for a given seed. Edge pack is 2-bit-per-channel quantisation to the `{0, 1/3, 2/3, 1}` lattice, bit-layout `L:0-1 | R:2-3 | T:4-5 | B:6-7`, saturated on out-of-range — intended for the companion R8 edge target that ASSAO-style blur will sample; 2 bits is lossy-by-design but enough weight for a separable pass. Symmetric rounding (`floor(x*3 + 0.5)`) so lattice points round-trip exactly. 12 unit tests in `tests/renderer/SsaoKernelTest.cpp`: kernel size/`+z`-hemisphere/unit-sphere invariants, seed determinism + differentiation, origin-bias check (first-quarter mean length < last-quarter), edge pack zero/full/per-channel-bit-position/saturation contracts, lattice-point round-trip across all 256 combinations, unpack-range guarantee across all 256 byte values. `ctest -R renderer` — 1/1 pass (503 s incl. Vulkan integration). GLSL mirrors + pipeline wiring deferred to RP.5b (main SSAO compute) and RP.5c (blur).
  - [x] RP.5b `ssao_main.comp` + `renderer::SsaoPipeline` — classic Chapman/LearnOpenGL hemisphere-kernel SSAO in a compute pass. Shape matches `DepthLinearizePipeline` / `DepthMipPipeline`: pipeline class owns its own `VkPipelineLayout` + `VkPipeline` directly (graphics-only `renderer::Pipeline` doesn't cover compute); deleted copy/move; `Bind(cmd)` at `VK_PIPELINE_BIND_POINT_COMPUTE`; layout cleanup on ctor failure. Descriptor set layout at set 0: `{0 = linear depth pyramid (combined-image-sampler), 1 = oct-packed view-space normal G-buffer (combined-image-sampler), 2 = SsaoUbo (uniform buffer — std140 `mat4 proj, mat4 invProj, vec4 kernel[64]`), 3 = R8 half-res AO (storage image)}`. Push constants: 16 bytes (`radius, bias, intensity, shadowPower`) — panel-tweakable knobs only; matrices + kernel live in the UBO to keep push constants well under 128 bytes. `SsaoPipeline::kKernelSize = 64` mirrors the `SSAO_KERNEL_SIZE` #define in the shader; both sides must move together. Shader runs in 8×8 tiles sized to the half-res output and: (1) samples center depth from mip 0 with an early-out at the `far`-sentinel (sky stays bright so multiply-composite leaves it untouched), (2) reconstructs view-space position via a self-contained GLSL mirror of `renderer::ReconstructViewPosFromDepth` (invProj → NDC z=1 → rescale so view.z = -linearDepth), (3) unpacks the view-space normal via a byte-identical mirror of `renderer::OctUnpackNormal`, (4) builds a per-pixel TBN rotated by interleaved-gradient noise (Jimenez 2014 — texture-free, blur wipes the pattern), (5) loops over the 64 hemisphere samples: offset in TBN space → project → compare linear depths with LearnOpenGL-style `smoothstep` range check (saturated at 1e-4 to dodge div-by-zero at the tap boundary), skipping out-of-frustum taps to avoid edge-clamp smear, (6) `pow(ao, shadowPower)` then remaps by `intensity` with a final `[0,1]` clamp so R8 quantises cleanly. 4 tests in `tests/renderer/SsaoPipelineTest.cpp`: shader SPIR-V compiled, constructs with valid handles, RAII destructor scope, push-constants size guard (`4*sizeof(float)`). `ctest -R renderer` — 1/1 pass (501 s incl. Vulkan integration). UBO/descriptor population + per-frame dispatch + `RenderLoop` wiring deferred to RP.5d.
  - [x] RP.5c `ssao_blur.comp` + `renderer::SsaoBlurPipeline` — separable edge-aware blur over the RP.5b half-res R8 AO target. Single shader + pipeline; two dispatches per frame with ping-ponged descriptor sets (H: `direction={1,0}` + ao target A→B, then V: `direction={0,1}` + ao target B→A). Shape mirrors `SsaoPipeline` (owns own layout+pipeline, deleted copy/move, `Bind(cmd)` at `VK_PIPELINE_BIND_POINT_COMPUTE`, ctor-failure layout cleanup). Descriptor set at set 0: `{0 = AO input (combined-image-sampler R8), 1 = linear depth pyramid (combined-image-sampler) for edge gating, 2 = R8 AO output (storage image)}`. Push constants: 12 bytes (`ivec2 direction, float edgeSharpness`) — tiny block so the two back-to-back dispatches only re-push the direction. Shader runs in 8×8 tiles, 7-tap symmetric gaussian (center + 3 pairs; weights `{0.265, 0.212, 0.108, 0.035}` sum to 1 pre-rejection). Edge rejection: per-tap weight is `baseWeight * exp2(-|Δd|/max(d, 0.01) * edgeSharpness)` — normalising by centre depth makes `edgeSharpness` a scale-invariant tuning knob, ε-guarded against the sky-sentinel `far` marker so the blur doesn't divide-by-near-zero over cleared pixels. Post-rejection weights are renormalised by the actual accumulated sum (crisp edges at the cost of `max(sum, 1e-4)` on pathologically-rejected pixels). Depth sampled at mip 0 to match `ssao_main.comp`'s tap; taps clamp to `[0, size-1]` so edge-of-screen pixels don't sample sampler-clamped neighbours and smear. 4 tests in `tests/renderer/SsaoBlurPipelineTest.cpp`: shader SPV exists, constructs with valid handles, RAII destructor scope, push-constants size guard (`2*sizeof(int32_t) + sizeof(float)`). `ctest -R renderer` — 1/1 pass (551 s incl. Vulkan integration). Per-frame dispatch + resource ping-pong + `RenderLoop` wiring deferred to RP.5d.
  - [x] RP.5d SSAO compute chain wired into `RenderLoop` + composited into tonemap. New public API: `RenderLoop::AO_FORMAT = VK_FORMAT_R8_UNORM`, `GetAoImageView(imageIndex)`, `SetProjection(proj, nearZ, farZ)` (replaces the RP.4d `SetProjectionNearFar` — SSAO needs the full projection matrix in its UBO, so near/far fold into the same call). Per swap image: two half-res R8_UNORM AO images (A = main output + V-blur output, B = H-blur intermediate) plus an `SsaoUbo` (`mat4 proj, mat4 invProj, vec4 kernel[64]`); kernel uploaded once in the ctor via `renderer::GenerateHemisphereKernel(64, 0)`, matrices refreshed each frame in `UploadSsaoUbo`. Descriptor layouts: main set (pyramid CIS, normal CIS, UBO, AO A storage) and blur set (AO src CIS, pyramid CIS, AO dst storage) both live for the `RenderLoop`'s lifetime; their pool + sets rebuild alongside HDR/normal/pyramid on resize/MSAA. `RunSsao(cmd)` runs in `EndFrame` after `BuildDepthPyramid`: A transitioned SHADER_READ→GENERAL, main SSAO dispatch → A, W→R barrier, H-blur dispatch A→B, W→R barrier on B + R→W barrier on A (guards the RAW on A for the coming V-write that the H-blur only hid by writing B), V-blur B→A, final GENERAL→SHADER_READ_ONLY_OPTIMAL on A for tonemap sample. MSAA path inherits the pyramid gate — `RunSsao` early-outs; tonemap still samples the AO binding, so `ClearAoImagesToWhite` (one-shot transfer submit in the ctor + on every rebuild) seeds A to 1.0 so the multiply is a no-op under MSAA. `tonemap.frag` gained `layout(set=0, binding=1) uniform sampler2D aoTexture` and multiplies `ao * hdr` into the ACES input; tonemap descriptor layout bumped to 2 CIS bindings (HDR + AO); `TonemapPipelineTest` updated to match. `Camera` grew two inline getters (`GetNearPlane`, `GetFarPlane`) so `main.cpp` can feed `SetProjection(camera.GetProjectionMatrix(), camera.GetNearPlane(), camera.GetFarPlane())` each frame without duplicating the perspective literals from `SetPerspective`. 4 new `RenderLoopTest` cases (`AoFormatIsR8Unorm`, `AoViewProvidedPerSwapImage`, `SsaoDispatchedDuringFrame` — full frame cycle exercises pyramid + 3 SSAO dispatches + tonemap sample under Vulkan validation, `SsaoSurvivesSampleCountChange` — MSAA-4× toggle: views stay valid + frame cycle works with SSAO gated off + tonemap multiplying by the pre-cleared 1.0). `ctest -R renderer` — 1/1 pass (544 s incl. Vulkan integration). Panel/tuning knobs for radius/intensity/shadowPower/edgeSharpness still deferred — currently hardcoded to `{0.5, 0.025, 1.0, 1.5}` for main SSAO and `{dir, 4.0}` for blur; RP.5's PLAN entry calls for a `RenderQualityPanel` section but that can land in a follow-up task if visual tuning surfaces new values.
- [x] RP.6 Selection + hover outline — single-bit stencil written in main pass, Sobel-on-id + depth-discontinuity screen-space outline pass; replaces fill-only vertex-colour highlight
  - [x] RP.6a CPU mirrors `renderer::SobelMagnitude(std::array<float,9>)` + `renderer::EdgeFromStencil(std::array<uint8_t,9>)`. New `renderer/OutlineEdge.{h,cpp}` following the `Tonemap.h` / `OctNormal.h` / `SsaoKernel.h` pattern. `SobelMagnitude` uses the standard Sobel kernels (Gx = [-1,0,1;-2,0,2;-1,0,1], Gy = [-1,-2,-1;0,0,0;1,2,1]) on a row-major 3×3 patch with the center at index 4; returns `sqrt(gx² + gy²)`. `EdgeFromStencil` returns 0 when all 9 stencil ids are equal (uniform interior or empty background), otherwise the max id present — so hover (2) beats selected (1) when both appear in the window, letting a cursor hovering over an already-selected element still render the hover colour instead of getting masked. 12 unit tests in `tests/renderer/OutlineEdgeTest.cpp`: Sobel zero on three flat patches (0, +1, -2), horizontal step → 4.0 (pins down Gx weights), vertical step → 4.0 (pins down Gy weights), diagonal step → sqrt(18) (exercises both kernels at once), sign invariance on a mixed-magnitude patch; edge-from-stencil flat-zero / flat-selected return 0, selected-on-background → 1, hovered-on-background → 2, hover-beats-selected → 2, edge-visible-from-background-side (center=0, neighbour=1) → 1 so the outline occupies a full 2-px band rather than a hairline, three-categories (0/1/2) → 2. `ctest -R renderer` — 1/1 pass (535 s incl. Vulkan integration). Shader (`outline.frag`) + pipeline wiring deferred to RP.6b; stencil/id attachment writes in main pass deferred to RP.6c; `RenderLoop` + panel wiring to RP.6d.
  - [x] RP.6c Outline stencil G-buffer attached to `RenderLoop`. New `RenderLoop::STENCIL_FORMAT = VK_FORMAT_R8_UINT` + `GetStencilImageView(imageIndex)` getter added. `CreateRenderPass` extended to a 7-slot MRT layout — non-MSAA `[hdr, normal, stencil, depth]`, MSAA `[hdr MSAA, normal MSAA, stencil MSAA, depth, hdr resolve, normal resolve, stencil resolve]`; `attachmentCount` picks 4 or 7 based on `multisampled`, `colorRefs` grew from 2 → 3 entries, `depthRef.attachment` shifted 2 → 3, `resolveRefs` grew to 3 entries at indices 4/5/6. Both the MSAA transient stencil attachment and the per-swap-image single-sample stencil target (COLOR_ATTACHMENT|SAMPLED) are allocated in `CreateHdrResources`/`CreateDepthResources` and torn down in lockstep with HDR/normal in `CleanupFrameResources`. Framebuffer build updated: attachments array sized to 7, non-MSAA writes 4 views, MSAA writes 7; `BeginFrame`'s clear values array bumped 5→7 with `clearValues[2].color.uint32[0] = 0u` (R8_UINT integer clear path — unwritten pixels fall into the background category 0) and depth shifted from slot 2 → slot 3. `basic.frag` gained `layout(location = 2) out uint outStencilId` which writes `0u` unconditionally — selection/hover plumbing via a push constant is deferred to RP.6d; emitting the constant today exercises the MRT pipeline wiring end-to-end so regressions show up here, not in RP.6d. Pipeline wiring flipped: scene pipelines (shaded/wireframe/transparent + CubeRenderTest) go from `colorAttachmentCount = 2, disableSecondaryColorWrites = true` → `colorAttachmentCount = 3, disableSecondaryColorWrites = false` since `basic.frag` now writes all three MRT outputs; overlay pipelines (section-fill, disk-marker) bump to `colorAttachmentCount = 3` but keep `disableSecondaryColorWrites = true` so attachments 1 and 2 stay at their clear values (oct-decodes to +Z for normal, 0 background for stencil) under the overlay fill. 3 new `RenderLoopTest` cases + a `BasicShaderTest` extension: `StencilFormatIsR8Uint`, `StencilGBufferImageViewsProvidedPerSwapImage`, `StencilGBufferSurvivesSampleCountChange` (MSAA-4× toggle exercises the 7-slot framebuffer + full frame cycle under Vulkan validation), `FragmentShaderDeclaresNormalOutputAtLocation1` extended to also require a third output at location 2 (SPIR-V OpDecorate Location + OpVariable Output walk). `CubeRenderTest.RenderCubeFrameWithoutErrors` binds `basic.frag` to the real `RenderLoop` render pass and covers the 3-attachment draw under validation — doubles as the MRT regression guard. Full `ctest -R renderer` — 1/1 pass (586 s incl. Vulkan integration). OutlinePipeline wiring + stencil push-constant plumbing + panel wiring deferred to RP.6d.
  - [x] RP.6b `outline.vert` + `outline.frag` + `renderer::OutlinePipeline` — graphics pipeline for the screen-space outline pass. Shape mirrors `TonemapPipeline`/`DiskMarkerPipeline` (owns `std::unique_ptr<renderer::Pipeline>`, deleted copy/move, CULL_NONE, no depth test, `alphaBlendEnable = true`, rasterization samples parametrised). `outline.vert` is the standard no-vertex-buffer fullscreen triangle (three NDC vertices derived from `gl_VertexIndex`, identical body to `tonemap.vert`). `outline.frag` at set 0 takes `{0 = stencil id (usampler2D, R8_UINT), 1 = linear depth (sampler2D, RP.4 pyramid mip 0)}` and a 48-byte push constant block (`vec4 selectedColor, vec4 hoverColor, vec2 texelSize, float thickness, float depthEdgeThreshold`) — panel-tweakable knobs only, well under the 128-byte Vulkan guarantee; C++ `OutlinePipeline::PushConstants` uses `alignas(16)` on the two colour vec4s so the memcpy layout matches the std140-like push-constant block byte-for-byte (offset 0 = selectedColor, 16 = hoverColor, 32 = texelSize, 40 = thickness, 44 = depthEdgeThreshold). GLSL helpers `edgeFromStencil(uint[9])` + `sobelMagnitude(float[9])` are byte-for-byte mirrors of the RP.6a CPU helpers — same row-major 3×3 patch layout, same min/max lattice rule (hover beats selected), same Sobel kernel weights. Shader logic: tap a 3×3 patch of stencil + linear depth offset by `thickness * texelSize`, `edgeFromStencil` → category; when category == 0 but `sPatch[4] > 0` (centre is inside a selected element) a Sobel-on-linear-depth > `depthEdgeThreshold` promotes the centre's category to outline — this is the "depth-discontinuity fallback" from the PLAN so within-selection silhouette creases still outline. Non-edge pixels `discard`; edge pixels emit `selectedColor` or `hoverColor` for the alpha-blend composite. Normal G-buffer not sampled in RP.6b — PLAN mentions it but the unit-test contract (SobelMagnitude + EdgeFromStencil only) doesn't require it and the current logic gets crisp outlines from stencil + depth alone; can be re-added if RP.6d visuals need crease-rejection. 6 Vulkan integration tests in `tests/renderer/OutlinePipelineTest.cpp`: SPIR-V presence for both shaders, `ConstructsWithValidHandles`, `ConstructsWithMsaa4x`, `DestructorCleansUp` (RAII scope), `PushConstantsSizeIsFortyEightBytes` (guards the 48-byte contract), `ColorOffsetsAreAlignedForStd140` (asserts offsets 0/16 — a wrong `alignas` attribute would silently drift the byte layout). `ctest -R renderer` — 1/1 pass (551 s incl. Vulkan integration). Stencil attachment writes + RenderLoop wiring deferred to RP.6c/d.
  - [x] RP.6d Wire OutlinePipeline + per-draw stencil push constant + panel controls. Three pieces in one commit (matches the RP.5d / RP.6c integrate-and-ship shape — splitting any one piece out lands an unconsumed shader change or a renderer reading an all-zero stencil). (1) `basic.frag` gained a fragment-stage push constant block at byte offset 64 (`layout(offset=64) uint stencilId;`) — sits cleanly after the existing 64-byte vertex-stage model matrix push range so the two stages never overlap and the combined block stays well under the 128-byte Vulkan guarantee. The shader writes `outStencilId = push.stencilId;` so per-draw selection state lands in the RP.6c R8_UINT G-buffer. (2) `main.cpp` adds a matching `VkPushConstantRange{FRAGMENT_BIT, offset=64, size=4}` to `pipelineConfig.pushConstantRanges` for the shaded/wireframe/transparent pipelines (overlay pipelines stay untouched — they don't bind basic.frag). New per-handle reverse map `handleToExpressIds` walks every node alongside the existing `expressIdToNodes` build so per-draw resolution is O(1) lookup + O(k) scan over the small id list (k = #expressIds sharing a handle, usually 1; large for batched walls). New `hoveredExpressId` cache subscribes to `bimeup::core::ElementHovered` (cheaper than re-raycasting per frame). New lambda `resolveStencilId(handle)` returns 2 (hovered beats selected — matches the `EdgeFromStencil` max rule baked into outline.frag), then 1 (any expressId in `selection.Contains`), then 0. Pushed via `vkCmdPushConstants(... FRAGMENT, 64, 4, &id)` after the existing model-matrix push in both opaque and transparent draw loops. (3) `RenderLoop` gains `m_outlineVertShader/m_outlineFragShader/m_outlinePipeline/m_outlineSampler/m_outlineSetLayout/m_outlineDescriptorPool/m_outlineDescriptorSets/m_outlinePush/m_outlineEnabled` plus `CreateOutlineDescriptors/CreateOutlinePipeline/UpdateOutlineDescriptors/CleanupOutlineDescriptors`. Sampler is linear, `CLAMP_TO_EDGE`, `maxLod = 0.25` so the depth-pyramid binding is implicitly clamped to mip 0 (the linearised-view-space depth the shader's Sobel-fallback wants — higher mips are the SSAO downsample chain, not what we sample here). Descriptor set layout: 2 fragment-stage CIS bindings (0 = stencil R8_UINT usampler2D, 1 = depth pyramid sampler2D), one set per swap image. Layout/pool/sampler live for the `RenderLoop`'s lifetime; descriptor sets and pipeline rebuild alongside HDR/normal/stencil/pyramid on `SetSampleCount` and `RecreateForSwapchain`. New public API `SetOutlineParams(const OutlinePipeline::PushConstants&, bool enabled)` lets the panel push knobs each frame; `texelSize` is overwritten in `EndFrame` with `(1/width, 1/height)` from the current swapchain extent so resizes track without a panel callback. Outline draw recorded between the tonemap fullscreen tri and the in-present-pass callback (so it composes over tonemapped colour but underneath ImGui), gated `m_outlineEnabled && m_samples == VK_SAMPLE_COUNT_1_BIT` — under MSAA the depth pyramid is gated off and the depth-edge fallback would sample an undefined image. (4) `RenderQualityPanel` gains an `OutlineSettings` struct (enable / selectedColor vec4 / hoverColor vec4 / thickness px / depthEdgeThreshold m) and a "Selection outline" `CollapsingHeader` (no `DefaultOpen` — matches existing panel style + user preference) wired to ColorEdit4 + SliderFloat widgets. `main.cpp` reads `panel.GetSettings().outline` each frame, fills `OutlinePipeline::PushConstants`, calls `renderLoop.SetOutlineParams(...)` right after `SetProjection`. Test split per the user-feedback "skip TDD on small GUI tweaks" rule + standard RenderLoop integration shape: (a) `BasicShaderTest.FragmentShaderDeclaresStencilIdPushConstant` walks SPIR-V `OpVariable PushConstant → OpTypePointer → OpTypeStruct` + `OpMemberDecorate Offset` to assert exactly one 32-bit unsigned int member exists at offset 64 (regression catches block removal, type drift, or offset drift before validation does); (b) `CubeRenderTest.RenderCubeFrameWithoutErrors` extended to declare the new fragment push range and push a non-zero stencilId — exercises the basic.frag layout shape main.cpp uses, validation flags any range mismatch; (c) 3 new `RenderLoopTest` cases (`OutlineDrawnDuringFrame` — full frame cycle with outline enabled under Vulkan validation; `OutlineDisabledStillCyclesFrame` — guards the `m_outlineEnabled = false` early-out path; `OutlineSurvivesSampleCountChange` — MSAA-4× toggle exercises the rebuild + the MSAA gate); panel widgets verified visually. `ctest --output-on-failure` (full renderer suite) — 330/330 pass (597 s); `ui_tests` — 131/131 pass. Stage RP RP.6 (Selection + hover outline) complete; next up RP.7 (SSIL).
- [x] RP.7 SSIL compute pass — port Godot/Intel `ssil.glsl` (one-bounce indirect colour from previous-frame HDR + reprojection); composite add before tonemap. Panel: enable / radius / intensity / normal-rejection
  - [x] RP.7a CPU mirrors `renderer::ComputeReprojectionMatrix(prevViewProj, currInvViewProj)` + `renderer::SsilNormalRejectionWeight(nCurr, nSampled, strength)`. New `renderer/SsilMath.{h,cpp}` following the `SsaoKernel.h` / `OutlineEdge.h` pattern. `ComputeReprojectionMatrix` returns `prevViewProj * currInvViewProj` — a current-frame clip point back-projects to world via `currInvViewProj` and then forward-projects through the previous frame's ViewProj, so `ssil_main.comp` can sample the previous HDR target at the reprojected UV; multiplication order is load-bearing and a swap would only be caught by the non-identity round-trip test. `SsilNormalRejectionWeight` returns `max(0, dot(nCurr, nSampled))^strength` — saturated cosine lobe raised by `strength` to match the Intel/Godot `ssil.glsl` reference and the "normal rejection" panel slider (higher strength narrows the acceptance lobe; strength = 0 disables rejection since `pow(0,0) = 1` by the C++ convention). 7 unit tests in `tests/renderer/SsilMathTest.cpp`: identity in → identity out; self round-trip (prev=curr) on a non-trivial perspective camera recovers the same clip point (pins multiplication order); moved-camera case reprojects a static world point from current clip to the prev-frame clip (the real contract `ssil_main.comp` relies on); normal rejection = 1 when normals match across strengths 1/2/8; = 0 at 90° across strengths 1/4; = 0 for back-facing (dot = -1) across strengths 1/2; strength sharpens lobe at 45° (dot^1, dot^2, dot^4 all match). `ctest -R renderer` — 1/1 pass (30.66 s). Shader + pipeline + RenderLoop wiring deferred to RP.7b–d.
  - [x] RP.7b `ssil_main.comp` + `renderer::SsilPipeline` — half-res R16G16B16A16_SFLOAT compute pass. Shape mirrors `SsaoPipeline` (owns own `VkPipelineLayout`+`VkPipeline`, deleted copy/move, `Bind(cmd)` at `VK_PIPELINE_BIND_POINT_COMPUTE`, layout cleanup on ctor failure). Descriptor set at set 0: `{0 = linear depth pyramid (CIS, sampled via textureLod), 1 = oct-packed view-space normal G-buffer (CIS), 2 = previous-frame HDR (CIS, rgba16f), 3 = SsilUbo (std140 uniform buffer — `mat4 proj, mat4 invProj, mat4 reprojection, vec4 kernel[64]`), 4 = RGBA16F half-res SSIL output (storage image, writeonly rgba16f)}`. Push constants: 16 bytes (`radius, intensity, normalRejection, frameSeed`) — panel-tweakable knobs only; matrices + kernel live in the UBO to keep push constants well under the 128-byte Vulkan guarantee. `SsilPipeline::kKernelSize = 64` mirrors the `SSIL_KERNEL_SIZE` #define in the shader; both sides must move together (SSIL + SSAO share the same hemisphere geometry from `renderer::GenerateHemisphereKernel(64, seed)` — RP.5a). Shader runs in 8×8 tiles sized to the half-res output: (1) samples centre depth from mip 0 with an early-out at the `far`-sentinel (sky emits zero — sky doesn't receive indirect bounces in this model; differs from SSAO which emits 1.0 for multiplicative composite), (2) reconstructs view-space position via a byte-identical GLSL mirror of `renderer::ReconstructViewPosFromDepth` / `ssao_main.comp`, (3) unpacks the view-space normal via a byte-identical mirror of `renderer::OctUnpackNormal`, (4) builds a per-pixel TBN rotated by IGN-jittered angle (Jimenez 2014) with `frameSeed` folded into the noise seed so the pattern drifts per frame — the RP.7c blur averages out the jitter, (5) loops over the 64 hemisphere samples: offset in TBN space → forward-project through current proj to find `currUv`, skip out-of-frustum taps (sampler-clamp-smear guard), (6) **normal rejection** — sample the tap's normal at `currUv` from the current-frame G-buffer and attenuate by `max(0, dot(nCurr, nSampled))^normalRejection` (GLSL mirror of `renderer::SsilNormalRejectionWeight` — RP.7a), skip when weight ≤ 0, (7) **reprojection** — multiply `currClip` by `ubo.reprojection` (= `prevViewProj * currInvViewProj` from `renderer::ComputeReprojectionMatrix`) to land in previous-frame clip directly, convert to `prevUv`, skip out-of-frustum, sample previous HDR at mip 0 and accumulate weighted, (8) output `(indirect / weightSum) * intensity` or 0 when no tap contributed. 4 tests in `tests/renderer/SsilPipelineTest.cpp` (same shape as `SsaoPipelineTest`): shader SPIR-V present, constructs with valid handles, RAII destructor scope, push-constants size guard (`4*sizeof(float)`). `ctest -R renderer` — 1/1 pass (31.23 s incl. Vulkan integration). UBO/descriptor population + per-frame dispatch + previous-HDR ring + `RenderLoop` wiring deferred to RP.7d; the separable blur comes in RP.7c.
  - [x] RP.7c `ssil_blur.comp` + `renderer::SsilBlurPipeline` — separable edge-aware blur over the RP.7b half-res RGBA16F SSIL target. Single shader + pipeline; two dispatches per frame with ping-ponged descriptor sets (H: `direction={1,0}` + SSIL target A→B, then V: `direction={0,1}` + SSIL target B→A). Shape mirrors `SsaoBlurPipeline` (owns own layout+pipeline, deleted copy/move, `Bind(cmd)` at `VK_PIPELINE_BIND_POINT_COMPUTE`, ctor-failure layout cleanup). Descriptor set at set 0: `{0 = SSIL input (CIS RGBA16F), 1 = linear depth pyramid (CIS) for silhouette gate, 2 = oct-packed view-space normal G-buffer (CIS) for crease gate, 3 = RGBA16F SSIL output (storage image)}` — adds a third CIS over the SSAO blur since colour bleeding across a crease is more visually offensive than AO bleed. Push constants: 16 bytes (`ivec2 direction, float edgeSharpness, float normalSharpness`) — 4 bytes more than `SsaoBlurPipeline::PushConstants` for the normal-lobe exponent; two back-to-back dispatches re-push only the direction half. Shader runs in 8×8 tiles, 7-tap symmetric gaussian (centre + 3 pairs; weights `{0.265, 0.212, 0.108, 0.035}` sum to 1 pre-rejection — same sigma ≈ 1.5 as `ssao_blur.comp`). Edge rejection: per-tap weight = `baseWeight * exp2(-|Δd|/max(d, 0.01) * edgeSharpness) * pow(max(0, dot(nCentre, nTap)), normalSharpness)` — depth-delta normalised by centre depth so `edgeSharpness` stays scale-invariant (ε-guarded against the sky-sentinel), plus a cosine lobe on the view-space normals unpacked via a byte-identical mirror of `renderer::OctUnpackNormal` / the other shaders' copies. Post-rejection weights renormalised by the actual accumulated sum (crisp edges at the cost of `max(sum, 1e-4)` on pathologically-rejected pixels). Depth sampled at mip 0 to match `ssil_main.comp`'s centre tap; taps clamp to `[0, size-1]` so edge-of-screen pixels don't sample sampler-clamped neighbours and smear bounce colour. 4 tests in `tests/renderer/SsilBlurPipelineTest.cpp` (same shape as `SsaoBlurPipelineTest`): shader SPV exists, constructs with valid handles, RAII destructor scope, push-constants size guard (`2*sizeof(int32_t) + 2*sizeof(float)`). `ctest -R renderer` — 1/1 pass (31.13 s incl. Vulkan integration). Per-frame dispatch + resource ping-pong + `RenderLoop` wiring deferred to RP.7d.
  - [x] RP.7d Wire SSIL into RenderLoop + add to tonemap composite. New public API: `RenderLoop::SSIL_FORMAT = VK_FORMAT_R16G16B16A16_SFLOAT`, `GetSsilImageView(imageIndex)`, `SetView(view)` (companion to `SetProjection` — SSIL reprojection needs the current view matrix), `SetSsilParams(radius, intensity, normalRejection, enabled)`. Per swap image: two half-res RGBA16F SSIL targets (A = main output + V-blur output, B = H-blur intermediate); one full-res RGBA16F prev-HDR image (1-frame ring; on resize / MSAA change rebuilt + cleared to 0 so the first frame's SSIL contribution is "no bounce"); one `SsilUbo` (`mat4 proj, mat4 invProj, mat4 reprojection, vec4 kernel[64]`) — kernel uploaded once in `CreateSsilResources` via `GenerateHemisphereKernel(64, 0)` (shared with SSAO's hemisphere), matrices refreshed each frame in `UploadSsilUbo` using the cached per-swap-image `prevViewProj`; one cached `prevViewProj[i]` updated after each `CopyHdrToPrevHdr`. Descriptor layouts (main = `{pyramid CIS, normal CIS, prev-HDR CIS, SsilUbo, SSIL A storage}`, blur = `{SSIL src CIS, pyramid CIS, normal CIS, SSIL dst storage}`) live for the `RenderLoop`'s lifetime; sets + pipeline rebuild on resize / MSAA along with HDR / normal / pyramid / SSAO. HDR image usage gained `VK_IMAGE_USAGE_TRANSFER_SRC_BIT` so `CopyHdrToPrevHdr` can snapshot it at end of frame. Dispatch order in `EndFrame`: scene → BuildDepthPyramid → RunSsao → **RunSsil** → present pass (tonemap + outline + ImGui) → **CopyHdrToPrevHdr** (outside any render pass; barriers HDR → TRANSFER_SRC, prev-HDR → TRANSFER_DST, vkCmdCopyImage, prev-HDR → SHADER_READ_ONLY, then `m_prevViewProj[i] = m_proj * m_view` for next cycle). RunSsil gates off under MSAA (inherits the pyramid gate) and when `m_ssilEnabled = false` — SSIL A stays at its cleared 0 so the tonemap add is a no-op; CopyHdrToPrevHdr still runs every frame so toggling SSIL on mid-session finds a valid prev-HDR. Barriers follow the SSAO template exactly: main pass A SHADER_READ→GENERAL (tonemap ended last frame), main dispatch → A, W→R on A, H-blur dispatch A→B, W→R on B + R→W on A, V-blur B→A, final A GENERAL→SHADER_READ_ONLY_OPTIMAL for tonemap. `tonemap.frag` gained a third combined-image-sampler at `set=0, binding=2` (SSIL); composite order: `aces((hdr + ssil) * ao)` — SSIL adds, AO multiplies. Tonemap descriptor pool+layout bumped to 3 bindings; `UpdateTonemapDescriptors` writes the SSIL binding via a lazy-init path matching the pre-existing AO one. `RenderQualityPanel` gained `SsilSettings` struct (`enabled / radius / intensity / normalRejection`) and a "SSIL" `CollapsingHeader` (no `DefaultOpen` — matches panel style + user preference) with Checkbox + SliderFloats; `main.cpp` calls `SetView(ubo.view)` and `SetSsilParams(...)` right after `SetProjection` each frame. 4 new tests per the PLAN split: (a) `TonemapPipelineTest.FragmentShaderDeclaresSsilBindingAtLocationTwo` walks SPIR-V `OpDecorate … Binding 2` so a layout regression fails here instead of at validation time; (b) 3 new `RenderLoopTest` cases (`SsilFormatIsR16g16b16a16Sfloat`; `SsilDispatchedDuringFrame` — full frame cycle with SSIL enabled under Vulkan validation, including the HDR→prev-HDR copy; `SsilSurvivesSampleCountChange` — MSAA-4× toggle exercises the rebuild + the MSAA gate). `TonemapPipelineTest` sampler layout bumped to 3 bindings. `ctest -R renderer` — 1/1 pass (31.95 s, 349 tests); full `ctest -j1 -E ApplicationTest` — 270/270 pass (33.59 s). Pre-existing `ApplicationTest` failures (shader path lookup unrelated to this task — `Failed to open SPIR-V file: /tonemap.vert.spv`) confirmed to predate the change. Panel widgets verified visually. Stage RP RP.7 (SSIL compute pass) complete; next up RP.8 (FXAA post).
- [x] RP.8 FXAA post — single fullscreen pass between outline and swapchain blit. Panel: enable + quality (LOW/HIGH)
  - [x] RP.8a CPU mirrors `renderer::FxaaLuminance(rgb)` + `renderer::FxaaLocalContrast(lumaC, lumaN, lumaS, lumaE, lumaW)` + `renderer::FxaaIsEdge(lumaC, N, S, E, W, edgeThreshold, edgeThresholdMin)`. New `renderer/FxaaMath.{h,cpp}` following the `Tonemap.h` / `SsilMath.h` pattern. `FxaaLuminance` uses Rec.709 weights `(0.2126, 0.7152, 0.0722)` — sum = 1 by construction so grayscale is a fixed point (flat-grey regions are never false-positive edges); modern standard, trivially bit-matchable by the shader port in RP.8b. `FxaaLocalContrast` is `max - min` over the 5-sample NESW+centre cross; centre must participate in both reductions so pixels where the centre is the darkest/brightest of the cross aren't misclassified as flat. `FxaaIsEdge` mirrors FXAA 3.11's early-exit predicate negated: `range >= max(edgeThresholdMin, lumaMax * edgeThreshold)` — absolute floor guards against false positives in dark regions (shot-noise gate), relative term makes bright regions need proportionally more contrast to register. 10 unit tests in `tests/renderer/FxaaMathTest.cpp`: luma of black → 0, white → 1, per-channel Rec.709 weights, grayscale fixed-point across 5 values; contrast flat-patch → 0 (three black/grey/white cases), centre = max / centre = min / centre-interior cases to pin centre participation; edge rejects three flat patches, detects full-range 1.0 step, dark-region floor (0.02 range in 0.05 luma stays flat), bright-region scaled gate (0.1 range at lumaMax=1.0 stays flat; 0.2 range is edge). `ctest -R renderer` — 1/1 pass (33.49 s incl. Vulkan integration). Shader + pipeline + RenderLoop wiring deferred to RP.8b/c.
  - [x] RP.8b `fxaa.vert` + `fxaa.frag` + `renderer::FxaaPipeline` — graphics fullscreen pass mirroring `TonemapPipeline` / `OutlinePipeline` shape (owns `std::unique_ptr<renderer::Pipeline>`, deleted copy/move, CULL_NONE, no depth test, `alphaBlendEnable = false` — FXAA is the final pixel write, overwrites the swapchain image directly). `fxaa.vert` is the standard no-vertex-buffer fullscreen triangle (identical body to `outline.vert` / `tonemap.vert`). `fxaa.frag` at set 0 takes `{0 = input LDR colour (CIS, sampler2D)}` — single binding, FXAA is pure colour-to-colour. Push constants: 24 bytes (`vec2 rcpFrame, float subpixel, float edgeThreshold, float edgeThresholdMin, int quality`) — all fields pack naturally with no padding under Vulkan push-constant rules (vec2 8-aligned at offset 0, scalars 4-aligned from offset 8); `quality` is an `int32` runtime knob (0=LOW/edge-only, 1=HIGH/edge+subpixel) rather than a specialization constant — the `renderer::Pipeline` class doesn't thread specialization info through today and a runtime branch on a single uniform value avoids the pipeline-rebuild flip a spec-constant would require. Shader is a simplified FXAA 3.11-style quality port: (1) 5-tap NESW+centre cross luma sample via `luma()` — byte-for-byte mirror of `renderer::FxaaLuminance` (Rec.709 weights, sum = 1 so grayscale is a fixed point), (2) early-exit `range < max(edgeThresholdMin, lumaMax * edgeThreshold)` — byte-for-byte mirror of `renderer::FxaaIsEdge` negated, (3) edge orientation via `|lN-lS| >= |lE-lW|` (perpendicular-gradient dominates; a horizontal edge shows a big vertical luma gradient), (4) neighbour pick on the side with the bigger luma jump, (5) 50/50 centre↔neighbour blend, (6) HIGH only — sub-pixel contribution: `subpixAmount = 1 - |avgLuma - lC|/range` clamped and scaled by `pc.subpixel`, then lerp toward the 5-tap average (softens sub-pixel features that the edge-only path leaves shimmering). Full FXAA 3.11 edge-walk search deferred — BIM scenes produce mostly polygon-silhouette aliasing, which the edge-orientation path handles well; visual quality verification at RP.8c. 6 Vulkan integration tests in `tests/renderer/FxaaPipelineTest.cpp` (same shape as `OutlinePipelineTest`): SPIR-V presence for both shaders, `ConstructsWithValidHandles`, `ConstructsWithMsaa4x` (future-proofs for direct-MSAA present), `DestructorCleansUp` (RAII scope), `SizeIsTwentyFourBytes` (guards the 24-byte contract), `FieldOffsetsMatchShaderLayout` (pins the exact byte offsets 0/8/12/16/20 so a reorder that still totals 24 gets caught here, not at validation time). Shader build is auto-globbed by `cmake/CompileShaders.cmake` — dropping `fxaa.{vert,frag}` into `assets/shaders/` was enough. `ctest -R renderer` — 1/1 pass (33.73 s incl. Vulkan integration). `RenderLoop` wiring + panel + composite order deferred to RP.8c.
  - [x] RP.8c Wire `FxaaPipeline` into `RenderLoop` + panel + `main.cpp` + stage-gate full ctest. **Architectural change**: the existing single "present" render pass (tonemap + outline + ImGui → swapchain) splits into two — a new `m_postRenderPass` (tonemap + outline → per-swap LDR intermediate, finalLayout `SHADER_READ_ONLY_OPTIMAL`), followed by `m_presentRenderPass` (FXAA + ImGui → swapchain). The two passes are intentionally Vulkan render-pass-compatible (same swapchain format, same sample count, single colour attachment, same attachment slots) so the existing tonemap + outline pipelines bind to the post pass unchanged — no pipeline rebuild required. New public API: `RenderLoop::SetFxaaParams(bool enabled, int quality)`. New private methods: `CreatePostRenderPass`, `CreateLdrResources`, `CreateFxaaDescriptors`, `CreateFxaaPipeline`, `UpdateFxaaDescriptors`, `CleanupFxaaDescriptors`. Per-swap resources: one LDR `VkImage` at swapchain format (`COLOR_ATTACHMENT | SAMPLED`), one view, one allocation; one post framebuffer per swap image; one FXAA descriptor set (single CIS binding at set 0/binding 0, sampler is linear + CLAMP_TO_EDGE + mip 0 only). Sampler/layout/pool live for the `RenderLoop`'s lifetime; LDR images + post framebuffers + FXAA descriptors rebuild alongside HDR/normal/stencil/pyramid/SSAO/SSIL in `SetSampleCount` / `RecreateForSwapchain`. **Dispatch flow** in `EndFrame`: end main pass → build pyramid → SSAO → SSIL → **begin post pass (LDR intermediate) → tonemap + outline → end post pass → begin present pass (swapchain) → FXAA + in-present callback (ImGui) → end present pass** → copy HDR to prev-HDR → submit + present. **Enable/disable model**: the FXAA draw runs every frame unconditionally (it's the only path from the LDR intermediate to the swapchain); when `m_fxaaEnabled = false` the push constants are set to `edgeThresholdMin = 1.0, edgeThreshold = 1.0` — both exceed the maximum possible luma range so the shader's early-exit fires on every pixel, reducing the work to a single sample + write. When enabled, LOW picks `{subpixel=0.5, edgeThreshold=0.25, edgeThresholdMin=0.0833, quality=0}` and HIGH picks `{subpixel=0.75, edgeThreshold=0.166, edgeThresholdMin=0.0625, quality=1}` — FXAA 3.11 reference defaults. `RenderQualityPanel` gained `FxaaSettings { enabled, quality }` and a "FXAA" `CollapsingHeader` (no `DefaultOpen` — matches existing panel style + user preference) with Checkbox + LOW/HIGH RadioButtons; `main.cpp` reads `panel.GetSettings().fxaa` each frame and calls `renderLoop.SetFxaaParams(...)` right after `SetOutlineParams`. 3 new `RenderLoopTest` cases (`FxaaDrawnDuringFrame` — enabled=true/quality=1 cycles frame under Vulkan validation; `FxaaDisabledStillCyclesFrame` — guards the always-on-draw shape: disabling mustn't leave the swapchain in an undefined state; `FxaaSurvivesSampleCountChange` — MSAA-4× toggle exercises the LDR + post-pass + FXAA-descriptor rebuild chain). `ctest -R renderer` — 1/1 pass (31.76 s, 352 tests). **Full stage gate** `ctest --output-on-failure -j1` — 270/274 pass; the 4 failures are pre-existing `ApplicationTest` SPIR-V-path issues documented in RP.7d (`Failed to open SPIR-V file: /tonemap.vert.spv` — unrelated to this change). Panel widgets verified visually. Stage RP RP.8 (FXAA post) complete; next up RP.9 (depth fog).
- [x] RP.9 Depth fog — `FogSettings` (colour + start/end), applied in tonemap frag. Panel: enable / colour / range
  - [x] RP.9a CPU mirror `renderer::ComputeFog(viewZ, start, end)`. New `renderer/FogMath.{h,cpp}` following the `FxaaMath.h` / `SsilMath.h` / `Tonemap.h` pattern. Returns `clamp((viewZ - start) / (end - start), 0, 1)` — standard linear distance-fog factor that `tonemap.frag` will feed into `mix(colour, fogColour, factor)`. Degenerate range (`end - start ≤ 1e-6`, panel sliders dragged together) degrades to a step function at `start` to avoid division-by-zero producing INF/NaN in the shader path; below returns 0, at-or-above returns 1. 9 unit tests in `tests/renderer/FogMathTest.cpp`: no-fog-before-start (three distances < start), factor = 0 exactly at start (seam guard), factor = 1 exactly at end (far-edge precision guard), saturates-beyond-end (clamps at 1 for 200/1000 units), linear-ramp midpoint → 0.5 (pins linearity — a quadratic/exponential ramp would fail this), quarter-and-three-quarter points → 0.25 / 0.75 (pins slope; a squared factor passes midpoint but fails these asymmetrically), negative viewZ clamps to 0 (guards numeric underflow at camera origin), strict monotonicity within range (catches a swap of start/end in the subtraction), degenerate-range step behaviour across below/at/above. `ctest -R renderer` — 1/1 pass (32.77 s incl. Vulkan integration); filtered `FogMathTest*` — 9/9. LightingUBO tail fields + GLSL mirror in `tonemap.frag` + panel wiring deferred to RP.9b.
  - [x] RP.9b Wire fog into `tonemap.frag` + `TonemapPipeline` + `RenderLoop` + panel + `main.cpp` + stage-gate full ctest. **Deviation from PLAN storage hint**: PLAN called for fog fields "in LightingUBO tail", but the established polish-pass pattern (RP.6d outline, RP.8c FXAA) is per-pipeline push constants rather than sharing the scene-pass LightingUBO — keeps tonemap from having to bind the main-pass UBO and leaves the 224-byte `LightingUbo` layout untouched. `FogSettings` still lives in `renderer/Lighting.h` where the PLAN places it conceptually. (1) `renderer/Lighting.h` gained a `FogSettings { enabled=false, color{0.55,0.60,0.70}, start=20, end=120 }` struct matching the PLAN "Expected APIs" block. (2) `TonemapPipeline::PushConstants` new struct at 24 bytes (`vec4 fogColorEnabled` rgb=color w=enabled-flag at offset 0, `float fogStart` at 16, `float fogEnd` at 20); `TonemapPipeline` constructor adds a `VkPushConstantRange{FRAGMENT, 0, 24}` to the pipeline layout. (3) `tonemap.frag` gained `layout(set=0, binding=3) uniform sampler2D linearDepthTexture;` (depth pyramid mip 0, view-space Z from RP.4), a fragment-stage push-constant block matching the 24-byte C++ struct, and a GLSL `computeFog()` byte-for-byte mirror of `renderer::ComputeFog` including the `denom ≤ 1e-6` step-function fallback. Fog applies **pre-ACES** as `lit = mix(lit, pc.fogColorEnabled.rgb, factor)` — panel-picked fog colour goes through the same tonemap curve as the sky/scene (the default fog colour matches the zenith so the fade blends with the hemisphere ambient). Gated by `pc.fogColorEnabled.w > 0.5` so a disabled flag short-circuits both the depth tap and the mix. (4) `RenderLoop` tonemap descriptor-set layout bumped 3 → 4 bindings; descriptor pool bumped `imageCount * 3 → imageCount * 4`; `UpdateTonemapDescriptors` writes the binding-3 depth sampler using the existing `m_depthPyramidSampledViews[i]` + `m_depthPyramidSampler` (tonemap only touches mip 0; the full sampled chain is fine to bind), with the same lazy-init pattern as AO/SSIL (null until the pyramid is created). New public API `SetFogParams(color, start, end, enabled)` rebuilds `m_fogPush` and force-clears the enable flag when `m_samples != VK_SAMPLE_COUNT_1_BIT` — the depth pyramid is gated off under MSAA (pyramid compute uses `sampler2D`, not `sampler2DMS`), so a sample at binding 3 would otherwise read undefined memory. `vkCmdPushConstants` uploads `m_fogPush` immediately before the tonemap fullscreen-tri draw. (5) `RenderQualityPanel` gained `renderer::FogSettings fog{}` field and a "Fog" `CollapsingHeader` (no `DefaultOpen` — matches panel style + user preference) with Checkbox + ColorEdit3 + Start/End SliderFloats (ranges 0–500 m / 0–1000 m). (6) `main.cpp` reads `panel.GetSettings().fog` each frame and calls `renderLoop.SetFogParams(color, start, end, enabled)` right after `SetFxaaParams`. Tests: (a) `TonemapPipelineTest.FragmentShaderDeclaresDepthBindingAtLocationThree` walks SPIR-V `OpDecorate … Binding 3` (regression catches a binding drop before validation time); (b) test fixture sampler layout bumped 3 → 4 bindings so `TonemapPipeline` validates against the real shader contract; (c) 2 new `TonemapPushConstantsTest` cases (`SizeIsTwentyFourBytes` guards the byte contract; `FieldOffsetsMatchShaderLayout` pins offsets 0/16/20 so a field reorder that still totals 24 bytes fails here, not at validation time); (d) 3 new `RenderLoopTest` cases (`FogAppliedDuringFrame` — full frame cycle with fog enabled under Vulkan validation, exercises the binding-3 depth sampler read; `FogDisabledStillCyclesFrame` — guards the shader's early-exit path so a regression that always sampled the pyramid on the first frame would trip validation; `FogSurvivesSampleCountChange` — MSAA-4× toggle exercises the tonemap-descriptor rebuild + the MSAA enable-flag force-clear). `ctest -R renderer` — 1/1 pass (32.98 s, 383 tests). **Full stage gate** `ctest -j1 --output-on-failure` — 270/274 pass; the 4 failures are the pre-existing `ApplicationTest` SPIR-V-path issues (`Failed to open SPIR-V file: /tonemap.vert.spv`) documented in RP.7d and RP.8c, unrelated to this change. Panel widgets verified visually. Stage RP RP.9 (Depth fog) complete; next up RP.10 (small-kernel bloom).
- [ ] RP.10 Small-kernel bloom — 3-mip dual-filter downsample/upsample + threshold + composite in tonemap. Panel: enable / threshold / intensity

## Stage 9 — Ray Tracing
- [ ] 9.1 Acceleration structures (BLAS)
- [ ] 9.2 Build TLAS
- [ ] 9.3 RT pipeline + SBT
- [ ] 9.4 RT ambient occlusion
- [ ] 9.5 RT soft shadows
- [ ] 9.6 RT reflections
- [ ] 9.7 Hybrid rendering
- [ ] 9.8 Toggle RT/rasterized via UI

## Stage 10 — VR Integration
- [ ] 10.1 OpenXR session lifecycle
- [ ] 10.2 Stereoscopic swapchain
- [ ] 10.3 Stereo rendering
- [ ] 10.4 Controller tracking + input
- [ ] 10.5 Teleport movement
- [ ] 10.6 VR ray interaction
- [ ] 10.7 VR UI panels
- [ ] 10.8 VR comfort features
- [ ] 10.9 Scale model gesture

## Stage 11 — Polish & Release
- [ ] 11.1 Drag-and-drop file loading
- [ ] 11.2 Recent files list
- [ ] 11.3 User preferences
- [ ] 11.4 Linux AppImage packaging
- [ ] 11.5 Windows NSIS installer
- [ ] 11.6 README with screenshots
- [ ] 11.7 Full integration test suite
- [ ] 11.8 Performance profiling pass
- [ ] 11.9 Accessibility review
